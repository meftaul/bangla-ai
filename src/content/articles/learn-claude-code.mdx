export const metadata = { title: "Using Coding Agents Effectively — Claude Code", description: "Web vs CLI, sharp prompts, the context window, CLAUDE.md, and skills — a live session on directing coding agents.", type: "slides" };

<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,340;0,9..144,560;1,9..144,340&family=IBM+Plex+Sans:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" />

<style>{`
/* Deck-local dark terminal theme. This style tag ships only with this article,
   so scoping under .reveal is already article-scoped. */
.reveal,
.reveal-viewport {
  background:
    radial-gradient(1100px 700px at 78% -10%, #1b1410 0%, transparent 60%),
    radial-gradient(900px 600px at -10% 110%, #10141a 0%, transparent 55%),
    #0c0d10;
}
.reveal {
  /* Re-scope the app tokens so Quiz/Poll/DragDrop follow the deck's dark theme
     regardless of the user's light/dark choice. */
  --foreground: #e9e7e1;
  --muted: #9a978f;
  --border: #2a2c34;
  --surface: #16181e;
  --accent: #e0764a;
  --accent-text: #e0764a;
  --accent-foreground: #16130f;
  --danger: #f26d5f;
  --human: #6ea8c7;
  --shadow-card: 0 12px 28px -14px rgb(0 0 0 / 0.6);
  font-family: "IBM Plex Sans", var(--font-sans), sans-serif;
  font-size: 27px;
  font-weight: 300;
  color: #e9e7e1;
}
.reveal .slides section { text-align: left; height: 100%; }

/* Typography */
.reveal .kicker {
  font-family: "JetBrains Mono", monospace;
  font-size: 0.52em;
  letter-spacing: 0.22em;
  color: #e0764a;
  text-transform: uppercase;
  margin: 0 0 0.5em;
}
.reveal .kicker .no { color: #9a978f; }
.reveal h1,
.reveal h2 {
  font-family: "Fraunces", var(--font-display), serif;
  font-weight: 560;
  color: #e9e7e1;
  text-transform: none;
}
.reveal h2 { font-size: 1.32em; line-height: 1.12; margin: 0 0 0.55em; letter-spacing: -0.01em; }
.reveal h2 em { font-style: italic; color: #e0764a; }
.reveal h1 { font-size: 2.15em; line-height: 1.05; margin: 0.1em 0 0.25em; letter-spacing: -0.015em; }
.reveal h1 em { font-style: italic; color: #e0764a; }

/* Split layout (points left/right of a terminal) */
.reveal .split { display: flex; gap: 38px; align-items: stretch; height: 100%; }
.reveal .points { flex: 0 0 44%; display: flex; flex-direction: column; justify-content: center; min-width: 0; }
.reveal .termwrap { flex: 1 1 56%; display: flex; flex-direction: column; justify-content: center; min-width: 0; }
.reveal .points ul { list-style: none; margin: 0; padding: 0; }
.reveal .points li {
  position: relative;
  padding: 0.5em 0 0.5em 1.15em;
  font-size: 0.62em;
  font-weight: 300;
  line-height: 1.45;
  color: #e9e7e1;
  border-bottom: 1px solid #2a2c34;
}
.reveal .points li:last-child { border-bottom: none; }
.reveal .points li::before {
  content: "";
  position: absolute;
  left: 0;
  top: 0.92em;
  width: 0.42em;
  height: 0.42em;
  background: #e0764a;
  border-radius: 1px;
  transform: rotate(45deg);
}
.reveal .points li strong { font-weight: 600; color: #fff; }
.reveal .points li .sub { display: block; color: #9a978f; font-size: 0.86em; margin-top: 0.15em; }

/* Terminal panel (markup rendered by the Terminal component) */
.reveal .term {
  background: #16181e;
  border: 1px solid #2a2c34;
  border-radius: 12px;
  box-shadow: 0 24px 60px -24px rgb(0 0 0 / 0.75);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  height: 520px;
  max-height: 100%;
}
.reveal .tbar {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px 13px;
  background: linear-gradient(#1c1e25, #171920);
  border-bottom: 1px solid #2a2c34;
  flex: none;
}
.reveal .dot { width: 10px; height: 10px; border-radius: 50%; }
.reveal .dot.r { background: #f26d5f; }
.reveal .dot.y { background: #f2be55; }
.reveal .dot.g { background: #57c76a; }
.reveal .ttl { margin-left: 9px; font-family: "JetBrains Mono", monospace; font-size: 12px; color: #9a978f; }
.reveal .tbody {
  flex: 1;
  overflow-y: auto;
  padding: 14px 16px 12px;
  font-family: "JetBrains Mono", monospace;
  font-size: 15px;
  line-height: 1.52;
  text-align: left;
}
.reveal .tl { display: flex; white-space: pre-wrap; word-break: break-word; }
.reveal .tl .pfx { flex: none; width: 1.4em; }
.reveal .tl-cmd { color: #f2f0ea; margin-top: 0.55em; }
.reveal .tl-cmd .pfx { color: #e0764a; }
.reveal .tl-ag { color: #cfccc4; margin-top: 0.45em; }
.reveal .tl-ag .pfx { color: #e0764a; }
.reveal .tl-out { color: #9a978f; }
.reveal .tl-dim { color: #6d6a63; }
.reveal .tl-ok { color: #7fd08a; }
.reveal .tl-warn { color: #e6b455; }
.reveal .caret {
  display: inline-block;
  width: 0.55em;
  height: 1.05em;
  background: #e0764a;
  vertical-align: -0.15em;
  margin-left: 2px;
  animation: deck-caret 1s steps(1) infinite;
}
@keyframes deck-caret { 50% { opacity: 0; } }

/* Title slide */
.reveal .titleslide .points { flex-basis: 46%; }
.reveal .lede { font-size: 0.56em; font-weight: 300; color: #9a978f; line-height: 1.55; max-width: 16em; }
.reveal .meta {
  font-family: "JetBrains Mono", monospace;
  font-size: 0.4em;
  color: #9a978f;
  margin-top: 2.2em;
  letter-spacing: 0.08em;
}
.reveal .meta b { color: #e0764a; font-weight: 500; }

/* Full-bleed statement slides */
.reveal .statement { display: flex; flex-direction: column; justify-content: center; align-items: flex-start; height: 100%; }
.reveal .statement h1 { font-size: 1.9em; max-width: 12em; }
.reveal .statement p { font-size: 0.62em; color: #9a978f; font-weight: 300; max-width: 26em; line-height: 1.55; }
.reveal .statement .mono { font-family: "JetBrains Mono", monospace; color: #e0764a; }
.reveal .rule { width: 72px; height: 3px; background: #e0764a; margin: 0 0 1.2em; }

/* Interactive activity slides */
.reveal .act { height: 100%; display: flex; flex-direction: column; justify-content: center; text-align: center; }
.reveal .act ul { list-style: none; margin-left: 0; padding-left: 0; }

/* reveal's white.css wins over globals.css in bundle order — recolor its chrome here
   (this style tag renders after every stylesheet, so it takes the cascade). */
.reveal .controls { color: #e0764a; }
.reveal .progress { color: #e0764a; }

/* ---- Layout additions (orange theme; agent = accent, human = cool) ---- */
.reveal .tag { display: inline-block; font-family: "JetBrains Mono", monospace; font-size: 0.38em; letter-spacing: 0.14em; text-transform: uppercase; padding: 5px 11px; border-radius: 999px; border: 1px solid #2a2c34; color: #9a978f; }
.reveal .tag.agent { color: #e0764a; border-color: rgba(224,118,74,0.4); background: rgba(224,118,74,0.08); }
.reveal .tag.human { color: #6ea8c7; border-color: rgba(110,168,199,0.4); background: rgba(110,168,199,0.08); }
.reveal .tiny { font-family: "JetBrains Mono", monospace; font-size: 0.43em; color: #9a978f; letter-spacing: 0.04em; }
.reveal .row { display: flex; gap: 12px; align-items: center; flex-wrap: wrap; }
.reveal .spacer { flex: 1; }
.reveal .grid2 { display: grid; grid-template-columns: 1fr 1fr; gap: 30px; align-items: center; align-content: center; height: 100%; }
.reveal .grid-side { display: grid; grid-template-columns: 0.85fr 1.15fr; gap: 36px; align-items: center; height: 100%; }
.reveal .card { border: 1px solid #2a2c34; border-radius: 14px; padding: 20px 22px; background: linear-gradient(180deg, rgba(255,255,255,0.025), transparent); }
.reveal .card.agent { border-color: rgba(224,118,74,0.3); }
.reveal .card.human { border-color: rgba(110,168,199,0.3); }
.reveal .card h3 { font-family: "Fraunces", serif; font-weight: 560; font-size: 0.8em; margin: 0 0 0.4em; color: #e9e7e1; }
.reveal .card p { font-size: 0.56em; color: #9a978f; line-height: 1.5; margin: 0; }
.reveal ul.ticks { list-style: none; margin: 0; padding: 0; font-size: 0.6em; }
.reveal ul.ticks li { padding: 6px 0; border-bottom: 1px dashed rgba(255,255,255,0.07); display: flex; gap: 10px; align-items: baseline; }
.reveal ul.ticks li:last-child { border-bottom: 0; }
.reveal ul.ticks li::before { content: "✓"; font-family: "JetBrains Mono", monospace; color: #e0764a; font-size: 0.9em; }
.reveal ul.ticks.human li::before { color: #6ea8c7; }
.reveal .flow { display: flex; flex-direction: column; gap: 0; }
.reveal .node { border: 1px solid #2a2c34; border-radius: 9px; padding: 9px 14px; font-family: "JetBrains Mono", monospace; font-size: 0.5em; letter-spacing: 0.04em; color: #9a978f; background: rgba(255,255,255,0.02); display: flex; justify-content: space-between; align-items: center; }
.reveal .node.on { border-color: rgba(224,118,74,0.5); color: #f2f0ea; background: rgba(224,118,74,0.07); }
.reveal .node.blocked { border-color: rgba(242,109,95,0.55); color: #f26d5f; background: rgba(242,109,95,0.08); }
.reveal .node .st { color: #e0764a; }
.reveal .node.blocked .st { color: #f26d5f; }
.reveal .arrow { width: 1px; height: 11px; background: #2a2c34; margin-left: 20px; }
.reveal .filecard { font-family: "JetBrains Mono", monospace; font-size: 0.48em; line-height: 1.62; border: 1px solid #2a2c34; border-radius: 10px; padding: 14px 16px; background: #0b0e13; }
.reveal .filecard .h { color: #9a978f; letter-spacing: 0.1em; text-transform: uppercase; font-size: 0.9em; margin-bottom: 8px; display: flex; justify-content: space-between; }
.reveal .filecard.bad { border-color: rgba(242,109,95,0.35); }
.reveal .filecard.good { border-color: rgba(127,208,138,0.35); }
.reveal .filecard .ok { color: #7fd08a; }
.reveal .filecard .err { color: #f26d5f; }
.reveal .filecard .muted { color: #6d6a63; }
.reveal .pull { font-family: "Fraunces", serif; font-size: 1em; font-weight: 560; line-height: 1.2; border-left: 3px solid #e0764a; padding-left: 0.6em; max-width: 22em; color: #e9e7e1; }
.reveal .pull span { color: #9a978f; }
.reveal .vague { font-family: "JetBrains Mono", monospace; font-size: 0.52em; color: #f26d5f; border: 1px dashed rgba(242,109,95,0.4); border-radius: 10px; padding: 14px 16px; background: rgba(242,109,95,0.05); }
.reveal .tcd { display: flex; flex-direction: column; gap: 10px; }
.reveal .tcd-row { display: grid; grid-template-columns: 52px 1fr; gap: 14px; border: 1px solid #2a2c34; border-radius: 12px; overflow: hidden; background: rgba(255,255,255,0.02); }
.reveal .tcd-row .k { display: flex; align-items: center; justify-content: center; font-family: "Fraunces", serif; font-size: 1.1em; font-weight: 560; background: rgba(224,118,74,0.08); color: #e0764a; }
.reveal .tcd-row .v { padding: 11px 14px; }
.reveal .tcd-row .v .kk { font-family: "JetBrains Mono", monospace; font-size: 0.42em; letter-spacing: 0.12em; text-transform: uppercase; color: #9a978f; margin-bottom: 4px; }
.reveal .tcd-row .v .vv { font-size: 0.56em; line-height: 1.4; color: #e9e7e1; }
.reveal .lead2 { font-size: 0.58em; color: #9a978f; line-height: 1.55; }
.reveal .mono { font-family: "JetBrains Mono", monospace; color: #e0764a; }

/* Context window visualizer (markup rendered by the ContextWindow component) */
.reveal .cw {
  background: #16181e;
  border: 1px solid #2a2c34;
  border-radius: 12px;
  box-shadow: 0 24px 60px -24px rgb(0 0 0 / 0.75);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  height: 520px;
  max-height: 100%;
}
.reveal .cwbody { flex: 1; overflow: hidden; padding: 14px 16px; display: flex; flex-direction: column; gap: 7px; }
.reveal .cwb { display: flex; justify-content: space-between; align-items: center; gap: 10px; font-family: "JetBrains Mono", monospace; font-size: 14.5px; padding: 9px 13px; border-radius: 8px; border: 1px solid #2a2c34; color: #e9e7e1; background: rgba(255,255,255,0.03); }
.reveal .cwb .lb { min-width: 0; }
.reveal .cwb .sz { flex: none; color: #9a978f; }
.reveal .cwb-sys { border-color: rgba(255,255,255,0.18); background: rgba(255,255,255,0.06); }
.reveal .cwb-md { border-color: rgba(230,180,85,0.4); background: rgba(230,180,85,0.07); color: #e6b455; }
.reveal .cwb-skill { border-color: rgba(224,118,74,0.5); background: rgba(224,118,74,0.09); color: #e0764a; }
.reveal .cwb-user { border-color: rgba(110,168,199,0.45); background: rgba(110,168,199,0.08); color: #6ea8c7; }
.reveal .cwb-tool { color: #9a978f; }
.reveal .cwb-in { animation: cwb-in 0.32s ease-out; }
.reveal .cwb-out { animation: cwb-out 0.3s ease-in forwards; }
@keyframes cwb-in { from { opacity: 0; transform: translateY(-9px) scale(0.97); } to { opacity: 1; transform: none; } }
@keyframes cwb-out { to { opacity: 0; transform: translateX(16px); } }
.reveal .cwfoot { display: flex; align-items: center; gap: 10px; padding: 12px 16px; border-top: 1px solid #2a2c34; background: linear-gradient(#171920, #1c1e25); flex: none; }
.reveal .cwmeter { flex: 1; height: 8px; border-radius: 99px; background: rgba(255,255,255,0.07); overflow: hidden; }
.reveal .cwmeter i { display: block; height: 100%; width: 0; border-radius: 99px; background: #e0764a; transition: width 0.3s ease; }
.reveal .cwmeter.hot i { background: #f26d5f; }
.reveal .cwpct { font-family: "JetBrains Mono", monospace; font-size: 12px; color: #9a978f; min-width: 3em; text-align: right; }
`}</style>

<Slide>

<div className="split titleslide">
  <div className="points">
    <p className="kicker">Episode 01 · Using coding agents effectively</p>
    <h1>Let's start with<br /><em>Claude Code</em>.</h1>
    <p className="lede">A working session on prompting, context windows, CLAUDE.md, and skills — for developers about to stop typing and start directing.</p>
    <div className="row" style={{ marginTop: "1.4em" }}><span className="tag agent">Agent</span><span className="tag human">Human</span><span className="tiny">Two colors. They mean something.</span></div>
  </div>
  <div className="termwrap">
    <Terminal
      title="~/todo-app — claude"
      steps={[[
        { t: "out", x: " ▐▛███▜▌   Claude Code" },
        { t: "out", x: "▝▜█████▛▘  ~/todo-app · 14 files" },
        { t: "cmd", x: "claude “let's get to work”" },
        { t: "ag", x: "Reading project…" },
        { t: "ok", x: "Ready." },
      ]]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="act">
  <p className="kicker">Warm-up · Live Poll</p>
  <Poll
    id="how-today"
    question="How do you use AI for coding today?"
    options={["Copy-paste into a web chat", "IDE autocomplete (Copilot etc.)", "A CLI agent in the terminal", "Not yet — that's why I'm here"]}
  />
</div>

</Slide>

<Slide>

<div className="grid-side">
  <div>
    <p className="kicker"><span className="no">01 /</span> Web chat vs CLI</p>
    <h2>Same request.<br />Two different <em>machines</em>.</h2>
    <p className="lead2">One of them hands you homework. The other one does the homework. You type the same three words into both.</p>
  </div>
  <div>
    <p className="tiny" style={{ marginBottom: "0.6em" }}>You type, into both:</p>
    <div className="vague" style={{ color: "#e9e7e1", borderColor: "#2a2c34", background: "rgba(255,255,255,0.02)" }}>Add authentication.</div>
    <p className="tiny" style={{ marginTop: "1.2em" }}>Watch the difference on the next slide.</p>
  </div>
</div>

</Slide>

<Slide>

<div className="grid2">
  <div>
    <div className="row" style={{ marginBottom: "0.6em" }}><span className="tag human">Web chat</span></div>
    <Terminal
      title="browser · chat"
      steps={[[
        { t: "cmd", x: "Add authentication." },
        { t: "out", x: "Sure! Here's an example implementation:" },
        { t: "out", x: '  import bcrypt from "bcrypt";' },
        { t: "out", x: "  export async function hashPassword(pw) {" },
        { t: "out", x: "    return bcrypt.hash(pw, 10);" },
        { t: "out", x: "  }  // …60 more lines" },
        { t: "warn", x: "  1. Create src/auth.ts" },
        { t: "warn", x: "  2. Copy the code above" },
        { t: "warn", x: "  3. npm install bcrypt" },
        { t: "warn", x: "  4. Wire it into your routes" },
        { t: "warn", x: "  5. Write the tests · 6. Run them" },
        { t: "warn", x: "you: copy → paste → run → paste error → repeat" },
      ]]}
    />
  </div>
  <div>
    <div className="row" style={{ marginBottom: "0.6em" }}><span className="tag agent">Claude Code</span></div>
    <Terminal
      title="zsh · ~/todo-app"
      steps={[[
        { t: "cmd", x: 'claude "Add authentication."' },
        { t: "ag", x: "Reading project…" },
        { t: "ok", x: "Read 14 files · express, prisma, vitest" },
        { t: "out", x: "  ✎ src/auth.ts        +62" },
        { t: "out", x: "  ✎ src/routes.ts      +8 -2" },
        { t: "out", x: "  ✎ tests/auth.test.ts +41" },
        { t: "ag", x: "Running tests…" },
        { t: "warn", x: "  1 failing · auth.test.ts:22" },
        { t: "ag", x: "Fixing, re-running…" },
        { t: "ok", x: "12 passing" },
        { t: "ok", x: "committed: feat: add authentication" },
        { t: "ok", x: "Done in 47s. Nothing for you to paste." },
      ]]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="grid2">
  <div className="card agent">
    <div className="row" style={{ marginBottom: "0.7em" }}><span className="tag agent">CLI agent</span><span className="spacer"></span><span className="tiny">does work</span></div>
    <ul className="ticks">
      <li className="fragment">Reads the entire project</li>
      <li className="fragment">Writes code to disk — no copy-paste</li>
      <li className="fragment">Runs the tests, fixes its own failures</li>
      <li className="fragment">Commits changes, reviewable line by line</li>
      <li className="fragment">Can run unattended — on a schedule, inside other tools</li>
    </ul>
  </div>
  <div className="card human">
    <div className="row" style={{ marginBottom: "0.7em" }}><span className="tag human">Web chat</span><span className="spacer"></span><span className="tiny">shapes thinking</span></div>
    <ul className="ticks human">
      <li className="fragment">Brainstorming</li>
      <li className="fragment">Learning something new</li>
      <li className="fragment">Quick questions</li>
      <li className="fragment">Architecture discussions</li>
    </ul>
    <p className="fragment" style={{ marginTop: "0.9em", fontSize: "0.5em", color: "#9a978f" }}>Right tool for the job — the terminal wins the moment the work is inside your own project.</p>
  </div>
</div>

</Slide>

<Slide>

<div className="act">
  <p className="kicker">Checkpoint · Quiz</p>
  <Quiz
    id="which-tool"
    question="You need to update 42 files across your repo. Which tool?"
    options={["Web chat — paste them in", "A CLI agent in the repo", "By hand over the weekend"]}
    answer={1}
  />
</div>

</Slide>

<Slide>

<div className="grid2">
  <div className="card">
    <div className="tag human" style={{ marginBottom: "0.8em" }}>Old workflow</div>
    <div className="flow">
      <div className="node">Think</div>
      <div className="arrow"></div>
      <div className="node">Write</div>
      <div className="arrow"></div>
      <div className="node">Debug</div>
      <div className="arrow"></div>
      <div className="node">Repeat</div>
    </div>
    <p className="tiny" style={{ marginTop: "0.9em" }}>Bottleneck: your hands.</p>
  </div>
  <div className="card agent fragment">
    <div className="tag agent" style={{ marginBottom: "0.8em" }}>New workflow</div>
    <div className="flow">
      <div className="node on">Think<span className="st">◆</span></div>
      <div className="arrow"></div>
      <div className="node on">Delegate<span className="st">◆</span></div>
      <div className="arrow"></div>
      <div className="node on">Review<span className="st">◆</span></div>
      <div className="arrow"></div>
      <div className="node on">Refine<span className="st">◆</span></div>
    </div>
    <p className="tiny" style={{ marginTop: "0.9em" }}>Bottleneck: your clarity.</p>
  </div>
  <p className="lead2 fragment" style={{ gridColumn: "1 / -1", margin: 0 }}>Your value shifts from fast typing to clear instructions. It's like leading a small team — and you check the result the way you'd review a new teammate's PR.</p>
</div>

</Slide>

<Slide>

<div className="split">
  <div className="points">
    <p className="kicker"><span className="no">03 /</span> Infer intent, not read minds</p>
    <h2>“Fix this” is not<br />a request. It's a <em>riddle</em>.</h2>
    <ul>
      <li className="fragment">The model has no eyes, no ticket, no standup.<span className="sub">Only the words you gave it and the files it can reach.</span></li>
      <li className="fragment">Vague in, guesses out.<span className="sub">The more precise the instruction, the fewer corrections you'll need.</span></li>
    </ul>
  </div>
  <div className="termwrap">
    <Terminal
      title="what does “this” mean"
      steps={[[
        { t: "cmd", x: "fix this." },
        { t: "warn", x: "Which file?" },
        { t: "warn", x: "Which behavior is wrong?" },
        { t: "warn", x: "Is it broken, or just ugly?" },
        { t: "warn", x: "Should I run the tests?" },
        { t: "warn", x: "Am I allowed to touch the middleware?" },
        { t: "warn", x: "Guessing…" },
        { t: "warn", x: "  ✎ 11 files changed. Probably not what you meant." },
      ]]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="grid-side">
  <div>
    <p className="kicker"><span className="no">04 /</span> Write good instructions</p>
    <h2 style={{ fontSize: "2.2em" }}>T · C · D</h2>
    <p className="lead2">Before you hit enter, check that your prompt names all three. Miss one and the agent guesses.</p>
  </div>
  <div className="tcd">
    <div className="tcd-row fragment"><div className="k">T</div><div className="v"><div className="kk">Task</div><div className="vv">What, concretely, do you want done. One outcome, stated as a verb.</div></div></div>
    <div className="tcd-row fragment"><div className="k">C</div><div className="v"><div className="kk">Context</div><div className="vv">The constraints, files, examples, or background only you know — and what it must <em>not</em> touch.</div></div></div>
    <div className="tcd-row fragment"><div className="k">D</div><div className="v"><div className="kk">Done signal</div><div className="vv">What does a good result look like? How would you — or the agent — check it?</div></div></div>
  </div>
</div>

</Slide>

<Slide>

<div className="act" style={{ textAlign: "left" }}>
  <p className="kicker"><span className="no">04 /</span> The same request, twice · coding</p>
  <div className="grid2" style={{ height: "auto", marginTop: "0.6em" }}>
    <div>
      <div className="filecard bad">
        <div className="h"><span>vague</span><span className="err">guesswork</span></div>
        add tests for foo.py
      </div>
      <p className="tiny" style={{ marginTop: "0.7em" }}>Which cases? Mocks allowed? How do we know it worked?</p>
    </div>
    <div>
      <div className="filecard good">
        <div className="h"><span>sharp</span><span className="ok">executable</span></div>
        <span className="fragment"><span className="ok">T</span> — write a test for foo.py covering the edge case where the user is logged out<br /></span>
        <span className="fragment"><span className="ok">C</span> — avoid mocks<br /></span>
        <span className="fragment"><span className="ok">D</span> — run the tests after; they pass</span>
      </div>
      <p className="tiny fragment" style={{ marginTop: "0.7em" }}>Same intent. Zero riddles.</p>
    </div>
  </div>
</div>

</Slide>

<Slide>

<div className="act" style={{ textAlign: "left" }}>
  <p className="kicker"><span className="no">04 /</span> The same request, twice · writing</p>
  <div className="grid2" style={{ height: "auto", marginTop: "0.6em" }}>
    <div>
      <div className="filecard bad">
        <div className="h"><span>vague</span><span className="err">guesswork</span></div>
        make this email better
      </div>
      <p className="tiny" style={{ marginTop: "0.7em" }}>Better how? For whom? How long? Ending on what?</p>
    </div>
    <div>
      <div className="filecard good">
        <div className="h"><span>sharp</span><span className="ok">executable</span></div>
        <span className="fragment"><span className="ok">T</span> — tighten this email to under 120 words<br /></span>
        <span className="fragment"><span className="ok">C</span> — busy client, warm but direct<br /></span>
        <span className="fragment"><span className="ok">D</span> — ends with one clear ask: a call next week</span>
      </div>
      <p className="tiny fragment" style={{ marginTop: "0.7em" }}>T·C·D works outside code, too.</p>
    </div>
  </div>
</div>

</Slide>

<Slide>

<div className="act">
  <p className="kicker">Checkpoint · Quiz</p>
  <Quiz
    id="spot-missing"
    question="“Fix the flaky login test in tests/auth.spec.ts. Do not touch the fixtures.” — Which part is missing?"
    options={["Task", "Context", "Done signal", "Nothing — it is complete"]}
    answer={2}
  />
</div>

</Slide>

<Slide>

<div className="split">
  <div className="points">
    <p className="kicker"><span className="no">05 /</span> The context window</p>
    <h2>Everything the agent<br />knows, <em>right now</em>.</h2>
    <ul>
      <li className="fragment">Your prompt goes in.<span className="sub">After the system prompt, CLAUDE.md, and skill descriptions.</span></li>
      <li className="fragment">Every file it reads goes in.<span className="sub">All 214 lines of it, on the desk.</span></li>
      <li className="fragment">Every test log and tool output goes in.</li>
      <li className="fragment">And the window is finite.<span className="sub">When it fills, old things fall off — silently.</span></li>
    </ul>
  </div>
  <div className="termwrap">
    <ContextWindow
      title="context · 200k window"
      steps={[
        [
          { op: "add", t: "sys", x: "System prompt", n: 8 },
          { op: "add", t: "md", x: "CLAUDE.md", n: 4 },
          { op: "add", t: "skill", x: "Skill descriptions × 10", n: 2 },
        ],
        [{ op: "add", t: "user", x: "Your prompt", n: 4 }],
        [{ op: "add", t: "tool", x: "@src/auth.ts · 214 lines", n: 16 }],
        [{ op: "add", t: "tool", x: "npm test output", n: 18 }],
        [
          { op: "add", t: "user", x: "Turns 2–9", n: 12 },
          { op: "add", t: "tool", x: "More file reads", n: 24 },
        ],
      ]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="act" style={{ textAlign: "left" }}>
  <p className="kicker"><span className="no">05 /</span> The consequence</p>
  <p className="pull" style={{ maxWidth: "24em" }}>When the window fills, the agent doesn't tell you what it forgot.<br /><span>It just starts being wrong with confidence.</span></p>
  <div className="grid2" style={{ marginTop: "1.2em", height: "auto" }}>
    <div className="card fragment"><h3 style={{ color: "#f26d5f" }}>Symptom</h3><p>It re-asks something you answered. It reverts a decision. It “forgets” a constraint from ten turns ago.</p></div>
    <div className="card fragment"><h3 style={{ color: "#7fd08a" }}>Cure</h3><p>Short standing instructions. Skills loaded on demand. And a <span className="mono">/clear</span> before the desk collapses.</p></div>
  </div>
</div>

</Slide>

<Slide>

<div className="split">
  <div className="points">
    <p className="kicker"><span className="no">06 /</span> CLAUDE.md — build one, live</p>
    <ul>
      <li className="fragment"><strong>Rules that apply to every conversation.</strong><span className="sub">The standing brief you never have to repeat.</span></li>
      <li className="fragment"><strong>Ruthlessly concise.</strong><span className="sub">It is paid for on every single turn.</span></li>
      <li className="fragment"><strong>Review it periodically.</strong><span className="sub">Stale rules mislead with authority.</span></li>
      <li className="fragment"><strong>Skip the obvious.</strong><span className="sub">The model can infer what any senior dev would.</span></li>
    </ul>
  </div>
  <div className="termwrap">
    <Terminal
      title="zsh · CLAUDE.md"
      steps={[
        [{ t: "cmd", x: "touch CLAUDE.md" }],
        [
          { t: "ok", x: "## Code style" },
          { t: "out", x: "- TypeScript strict. No `any`. Ever." },
          { t: "out", x: "- Named exports only." },
        ],
        [
          { t: "ok", x: "## Testing" },
          { t: "out", x: "- `npm test` must pass before any commit." },
          { t: "dim", x: "(one line — not a testing philosophy essay)" },
        ],
        [
          { t: "ok", x: "## Never" },
          { t: "out", x: "- Edit src/generated/** · Commit to main" },
        ],
        [{ t: "dim", x: "Saved. 180 tokens. Read on every single turn." }],
      ]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="grid2">
  <div>
    <div className="filecard bad">
      <div className="h"><span>CLAUDE.md · verbose</span><span className="err">2,400 tokens</span></div>
      This project was started in 2021 by a team of four engineers who wanted to build a modern todo application using best practices. We believe strongly in clean code, and we have found over the years that… <span className="muted">…41 more paragraphs</span>
    </div>
    <p className="tiny" style={{ marginTop: "0.7em" }}>Eats the desk. Gets skimmed. Ages badly.</p>
  </div>
  <div>
    <div className="filecard good">
      <div className="h"><span>CLAUDE.md · sharp</span><span className="ok">180 tokens</span></div>
      <span className="ok">## Code style</span><br />- TypeScript strict. No `any`.<br />- Named exports only.<br /><br /><span className="ok">## Never</span><br />- Edit src/generated/**<br />- Commit to main directly
    </div>
    <p className="tiny" style={{ marginTop: "0.7em" }}>Rules, not prose. Bans, not vibes.</p>
  </div>
</div>

</Slide>

<Slide>

<div className="split">
  <div className="points">
    <p className="kicker"><span className="no">07 /</span> Skills</p>
    <h2>Knowledge that<br />waits on <em>disk</em>.</h2>
    <ul>
      <li className="fragment">You ask for something the skill covers.<span className="sub">Until now, only its one-line description rode along.</span></li>
      <li className="fragment">The agent reads SKILL.md from disk — only now.<span className="sub">Watch it slide into the window.</span></li>
      <li className="fragment">Used, then put back.<span className="sub">No permanent rent on the window.</span></li>
    </ul>
  </div>
  <div className="termwrap">
    <ContextWindow
      title="context · 10 skills installed"
      steps={[
        [
          { op: "add", t: "sys", x: "System prompt", n: 8 },
          { op: "add", t: "md", x: "CLAUDE.md", n: 4 },
          { op: "add", t: "skill", x: "Skill descriptions × 10", n: 3 },
        ],
        [{ op: "add", t: "user", x: "“make the dashboard faster”", n: 4 }],
        [{ op: "add", t: "skill", x: "SKILL.md: tcd — read from disk", n: 6 }],
        [{ op: "add", t: "tool", x: "Prompt rewritten as T·C·D", n: 5 }],
      ]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="grid2">
  <div className="card fragment"><h3>Lives on disk</h3><p>Version it. Review it. Share it across the team like any other file.</p></div>
  <div className="card fragment"><h3>Loaded only when needed</h3><p>Ten skills installed, one line of description each until one is actually invoked.</p></div>
  <div className="card agent fragment" style={{ gridColumn: "1 / -1" }}><h3>Doesn't consume permanent context</h3><p>CLAUDE.md is rent you pay every turn. A skill is a book you take off the shelf, read, and put back.</p></div>
</div>

</Slide>

<Slide>

<div className="act">
  <p className="kicker">Checkpoint · Quiz</p>
  <Quiz
    id="skill-cost"
    question="You have 10 skills installed. What do they cost in context before any is invoked?"
    options={["All ten SKILL.md bodies", "Nothing at all", "One line of description each", "Depends on the model"]}
    answer={2}
  />
</div>

</Slide>

<Slide>

<div className="split">
  <div className="points">
    <p className="kicker"><span className="no">08 /</span> Build our first skill</p>
    <ul>
      <li className="fragment"><strong>1 · Make the folder</strong><span className="sub">.claude/skills/&lt;name&gt;/SKILL.md — the filename is the contract.</span></li>
      <li className="fragment"><strong>2 · Write the frontmatter</strong><span className="sub">name + description: how the agent decides when to reach for this. Write it for a stranger.</span></li>
      <li className="fragment"><strong>3 · Write the body</strong><span className="sub">The actual instructions, in plain T·C·D language.</span></li>
      <li className="fragment"><strong>4 · It registers</strong><span className="sub">No restart, no build. It waits on disk.</span></li>
    </ul>
  </div>
  <div className="termwrap">
    <Terminal
      title="zsh · ~/todo-app"
      steps={[
        [{ t: "dim", x: "goal: a skill that rewrites vague prompts as T·C·D" }],
        [
          { t: "cmd", x: "mkdir -p .claude/skills/tcd" },
          { t: "cmd", x: "touch .claude/skills/tcd/SKILL.md" },
          { t: "ok", x: "  ✓ .claude/skills/tcd/SKILL.md" },
        ],
        [
          { t: "dim", x: "---" },
          { t: "out", x: "name: tcd" },
          { t: "out", x: "description: Rewrite a vague prompt into Task," },
          { t: "out", x: "  Context, and Done signal. Use when a request" },
          { t: "out", x: "  lacks files, constraints, or success criteria." },
          { t: "dim", x: "---" },
        ],
        [
          { t: "out", x: "Rewrite $PROMPT as three labeled sections." },
          { t: "out", x: "Never invent files. If context is missing, ask." },
        ],
        [{ t: "ok", x: "Skill registered. One line of context until invoked." }],
      ]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="split">
  <div className="points">
    <p className="kicker"><span className="no">08 /</span> Run it</p>
    <p className="lead2">Same intent you'd have typed anyway — now rewritten into something the agent can execute without guessing.</p>
  </div>
  <div className="termwrap">
    <Terminal
      title="claude · /skill tcd"
      steps={[[
        { t: "cmd", x: "/skill tcd" },
        { t: "cmd", x: "make the dashboard faster" },
        { t: "ag", x: "Loading skill: tcd…" },
        { t: "ok", x: "SKILL.md read · 0 permanent context" },
        { t: "ok", x: "TASK" },
        { t: "out", x: "  Reduce time-to-interactive on /dashboard below 1.5s." },
        { t: "ok", x: "CONTEXT" },
        { t: "out", x: "  See src/pages/Dashboard.tsx, src/api/metrics.ts." },
        { t: "out", x: "  Do not change the public API of useMetrics()." },
        { t: "ok", x: "DONE SIGNAL" },
        { t: "out", x: "  `npm run bench:dashboard` reports TTI < 1500ms." },
        { t: "dim", x: "Same intent. Now executable." },
      ]]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="split">
  <div className="points">
    <p className="kicker"><span className="no">09 /</span> Managing the session</p>
    <h2>Conversations <em>rot</em>.</h2>
    <ul>
      <li className="fragment">Corrections and dead ends stay on the desk.<span className="sub">Turn 19: it re-asks a question you answered.</span></li>
      <li className="fragment">And keep influencing every next answer.<span className="sub">Turn 23: it reverts a decision from turn 6.</span></li>
      <li className="fragment"><span className="mono">/clear</span> between unrelated tasks.<span className="sub">CLAUDE.md and skills survive. The rot does not.</span></li>
    </ul>
  </div>
  <div className="termwrap">
    <ContextWindow
      title="context · session, hour two"
      steps={[
        [
          { op: "add", t: "sys", x: "System prompt", n: 8 },
          { op: "add", t: "md", x: "CLAUDE.md", n: 4 },
          { op: "add", t: "skill", x: "Skill descriptions", n: 2 },
          { op: "add", t: "tool", x: "Migration debugging · turns 1–14", n: 38 },
          { op: "add", t: "user", x: "Corrections × 3", n: 12 },
          { op: "add", t: "tool", x: "Abandoned approach", n: 22 },
        ],
        [{ op: "add", t: "tool", x: "Turn 19 · re-asks an answered question", n: 6 }],
        [{ op: "add", t: "tool", x: "Turn 23 · reverts a good decision", n: 5 }],
        [
          { op: "clear" },
          { op: "add", t: "sys", x: "System prompt", n: 8 },
          { op: "add", t: "md", x: "CLAUDE.md", n: 4 },
          { op: "add", t: "skill", x: "Skill descriptions", n: 2 },
        ],
      ]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="grid-side">
  <div className="flow">
    <div className="node">Wrong<span className="st">◆</span></div>
    <div className="arrow"></div>
    <div className="node">Correction<span className="st">◆</span></div>
    <div className="arrow"></div>
    <div className="node">Still wrong<span className="st">◆</span></div>
    <div className="arrow"></div>
    <div className="node blocked">RESET · /clear<span className="st">■</span></div>
    <div className="arrow"></div>
    <div className="node on">Fresh prompt, better written<span className="st">◆</span></div>
  </div>
  <div>
    <p className="kicker"><span className="no">09 /</span> The two-correction rule</p>
    <p className="pull">If two corrections haven't fixed it,<br /><span>the third one won't either.</span></p>
    <p className="lead2" style={{ marginTop: "1em" }}>A third correction isn't more information — it's more noise on a prompt that was wrong from the start. Course-correct early; don't let a wrong turn compound. Kill the session, rewrite with T·C·D.</p>
  </div>
</div>

</Slide>

<Slide>

<div className="act">
  <p className="kicker">Checkpoint · Quiz</p>
  <Quiz
    id="context-reset"
    question="You just spent 40 minutes debugging a migration. Now you want a fresh, unrelated endpoint. First thing you type?"
    options={["Just describe the endpoint", "/compact, then describe it", "/clear, then describe it", "Restart your terminal"]}
    answer={2}
  />
</div>

</Slide>

<Slide>

<div className="split">
  <div className="points">
    <p className="kicker"><span className="no">10 /</span> External context · Files</p>
    <p className="lead2">Point at a file with <span className="mono">@</span>. The agent reads it from disk — not from your clipboard, not from your description of it.</p>
  </div>
  <div className="termwrap">
    <Terminal
      title="@src/auth.ts"
      steps={[[
        { t: "cmd", x: "Why does @src/auth.ts reject valid tokens?" },
        { t: "ag", x: "Reading src/auth.ts…" },
        { t: "ok", x: "214 lines" },
        { t: "out", x: "  Line 88: exp compared in ms, issued in s." },
        { t: "warn", x: "  Every token looks expired." },
        { t: "dim", x: "It read the file. You pasted nothing." },
      ]]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="split">
  <div className="points">
    <p className="kicker"><span className="no">10 /</span> External context · Images</p>
    <p className="lead2">Drop a screenshot. Circle the broken thing. A picture carries what a sentence can't — and the agent now talks about <em>your</em> button, in <em>your</em> component.</p>
  </div>
  <div className="termwrap">
    <Terminal
      title="screenshot.png"
      steps={[[
        { t: "cmd", x: "[screenshot.png] the badge overlaps on mobile" },
        { t: "ag", x: "Inspecting image…" },
        { t: "ok", x: "Found: <NotificationBadge />" },
        { t: "out", x: "  Overlap at <390px. Absolute badge, no min-width." },
        { t: "out", x: "  → src/components/NotificationBadge.tsx:31" },
        { t: "dim", x: "It named your component. Not a generic one." },
      ]]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="split">
  <div className="points">
    <p className="kicker"><span className="no">10 /</span> External context · URLs</p>
    <p className="lead2">Paste the docs URL. It cites the actual signature from the actual version — instead of the one it half-remembers. Allowlist frequent domains with <span className="mono">/permissions</span>.</p>
  </div>
  <div className="termwrap">
    <Terminal
      title="fetch docs"
      steps={[[
        { t: "cmd", x: "Use https://docs.stripe.com/api/charges/create" },
        { t: "ag", x: "Fetching documentation…" },
        { t: "ok", x: "Read current API reference" },
        { t: "out", x: "  Required: amount, currency, source" },
        { t: "warn", x: "  Note: `capture` deprecated in this version." },
        { t: "dim", x: "Cited, not remembered." },
      ]]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="split">
  <div className="points">
    <p className="kicker"><span className="no">10 /</span> External context · Raw data</p>
    <p className="lead2">Pipe it. 40,000 lines of log you'll never scroll through, read in one gulp. No copy-paste, no truncation.</p>
  </div>
  <div className="termwrap">
    <Terminal
      title="cat server.log | claude"
      steps={[[
        { t: "cmd", x: 'cat server.log | claude -p "find the root cause"' },
        { t: "ag", x: "Streaming 41,204 lines…" },
        { t: "ok", x: "Ingested" },
        { t: "warn", x: "  17:42:03 pool exhausted (max=10)" },
        { t: "dim", x: "  …repeated 1,882 times" },
        { t: "out", x: "  Root cause: connections never released in" },
        { t: "out", x: "  OrderRepository.findAll() on the error path." },
        { t: "dim", x: "No clipboard was harmed." },
      ]]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="split">
  <div className="points">
    <p className="kicker"><span className="no">11 /</span> Workflow one — a new endpoint</p>
    <h2>Encode your <em>architecture</em>.</h2>
    <ul>
      <li className="fragment"><strong>1 · Make the folder</strong><span className="sub">.claude/skills/endpoint/SKILL.md</span></li>
      <li className="fragment"><strong>2 · Describe when to use it</strong><span className="sub">“Use when asked to add an endpoint or route.”</span></li>
      <li className="fragment"><strong>3 · Spell out the layers, in order</strong><span className="sub">Controller → Command → Handler → Service → Repository. Your team's shape.</span></li>
      <li className="fragment"><strong>4 · Name the done signal</strong><span className="sub">Tests written, npm test passing, committed.</span></li>
    </ul>
  </div>
  <div className="termwrap">
    <Terminal
      title="zsh · ~/shop-api"
      steps={[
        [{ t: "dim", x: "goal: every new endpoint follows our layered architecture" }],
        [
          { t: "cmd", x: "mkdir -p .claude/skills/endpoint" },
          { t: "cmd", x: "touch .claude/skills/endpoint/SKILL.md" },
          { t: "ok", x: "  ✓ .claude/skills/endpoint/SKILL.md" },
        ],
        [
          { t: "dim", x: "---" },
          { t: "out", x: "name: endpoint" },
          { t: "out", x: "description: Scaffold a new HTTP endpoint as a" },
          { t: "out", x: "  full vertical slice. Use when asked to add an" },
          { t: "out", x: "  endpoint or route." },
          { t: "dim", x: "---" },
        ],
        [
          { t: "out", x: "Create these layers, in this order:" },
          { t: "out", x: "1. Controller — receives the HTTP request" },
          { t: "out", x: "2. Command — the validated intent" },
          { t: "out", x: "3. Command Handler — orchestrates the use case" },
          { t: "out", x: "4. Service — the domain logic" },
          { t: "out", x: "5. Repository — talks to the database" },
        ],
        [
          { t: "out", x: "Write tests for the new endpoint." },
          { t: "out", x: "Done when `npm test` passes. Then commit." },
          { t: "ok", x: "Skill registered." },
        ],
      ]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="grid-side">
  <div>
    <p className="kicker"><span className="no">11 /</span> Workflow one — the file</p>
    <h2>The whole skill.<br />One <em>readable</em> file.</h2>
    <p className="lead2">Frontmatter tells the agent <em>when</em> to reach for it. The body tells it <em>what</em> to do. Notice it's just T·C·D, written down once for the whole team.</p>
  </div>
  <div>
    <div className="filecard good">
      <div className="h"><span>.claude/skills/endpoint/SKILL.md</span><span className="ok">~120 tokens</span></div>
      <span className="muted">---</span><br />name: endpoint<br />description: Scaffold a new HTTP endpoint as a full<br />&nbsp;&nbsp;vertical slice. Use when asked to add an endpoint<br />&nbsp;&nbsp;or route.<br /><span className="muted">---</span><br /><br /><span className="ok">Create these layers, in this order:</span><br />1. Controller — receives the HTTP request<br />2. Command — the validated intent<br />3. Command Handler — orchestrates the use case<br />4. Service — the domain logic<br />5. Repository — talks to the database<br /><br /><span className="ok">Write tests for the new endpoint.</span><br />Done when `npm test` passes. Then commit.
    </div>
  </div>
</div>

</Slide>

<Slide>

<div className="grid-side">
  <div>
    <p className="kicker"><span className="no">11 /</span> Workflow one — run it</p>
    <p className="lead2">One vague sentence in. The whole vertical slice out — in your team's shape, in your team's order, tests included.</p>
  </div>
  <div className="termwrap">
    <Terminal
      title="claude · /skill endpoint"
      steps={[[
        { t: "cmd", x: "/skill endpoint" },
        { t: "cmd", x: "add a POST /orders endpoint" },
        { t: "ag", x: "Loading skill: endpoint…" },
        { t: "ok", x: "SKILL.md read · creating the slice, in order" },
        { t: "ok", x: "created src/http/OrderController.ts" },
        { t: "ok", x: "created src/app/CreateOrderCommand.ts" },
        { t: "ok", x: "created src/app/CreateOrderHandler.ts" },
        { t: "ok", x: "created src/domain/OrderService.ts" },
        { t: "ok", x: "created src/infra/OrderRepository.ts" },
        { t: "ok", x: "created tests/orders.spec.ts" },
        { t: "ok", x: "npm test → 23 passing" },
        { t: "ok", x: 'git commit -m "feat(orders): create endpoint"' },
      ]]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="act">
  <p className="kicker">Hands-on · Put it in order</p>
  <DragDrop
    id="endpoint-order"
    prompt="Order the layers the endpoint skill creates — request first"
    items={["Controller — receives the HTTP request", "Command — the validated intent", "Command Handler — orchestrates the use case", "Service — the domain logic", "Repository — talks to the database"]}
  />
</div>

</Slide>

<Slide>

<div className="grid-side">
  <div>
    <p className="kicker"><span className="no">11 /</span> Workflow two — bug investigation</p>
    <p className="lead2">This one interviews you first. It refuses to guess — it clarifies, locates the exact bug, and plans before touching a file.</p>
  </div>
  <div className="termwrap">
    <Terminal
      title="claude · /skill triage"
      steps={[[
        { t: "cmd", x: "checkout hangs after coupon applied" },
        { t: "ag", x: "interview: which coupon? which env? reproducible?" },
        { t: "cmd", x: "SUMMER24, staging, always" },
        { t: "out", x: "located src/pricing/coupon.ts, src/checkout/flow.ts" },
        { t: "out", x: "plan: 3 steps, 2 files, no schema change" },
        { t: "ag", x: "applied fix — awaited promise never resolved" },
        { t: "ok", x: "npm test → 47 passing" },
        { t: "ok", x: "summary: 1 root cause, 2 files, 0 side effects" },
      ]]}
    />
  </div>
</div>

</Slide>

<Slide>

<div className="act">
  <p className="kicker">Last word is yours · Poll</p>
  <Poll
    id="first-skill"
    question="Which team workflow deserves a skill first?"
    options={["Feature testing", "Release checklist", "Code review", "Onboarding docs"]}
  />
</div>

</Slide>

<Slide>

<div className="statement">
  <p className="kicker">Episode 01 · Ends here</p>
  <div className="rule"></div>
  <h1>The future isn't about<br />replacing developers.<br />It's about becoming an effective <em>leader</em> of AI software engineers.</h1>
  <div className="row" style={{ marginTop: "1.4em" }}><span className="tag agent">Agent</span><span className="tag human">Human</span><span className="tiny">You were always the one in the second color.</span></div>
</div>

</Slide>

export const metadata = {
  title: "Math for AI 5.1 — Span, which remote reaches where",
  description: "Moving day, and Shiku's remote is broken. The toy shop has four kinds of two-button remote. Which of them take Shiku from the door to all four of Ammu's chalk marks? Nasib says two buttons is always enough.",
  type: "article",
};

{/* Told as a Brilliant-style journey, like 1.1 and 2.3: screen 1 seals the
    reader's bet on which of the four remotes reach every chalk mark, and the
    bet is not settled until the Finale, nine screens later, after PaintReach
    and YourRemotes have shown that a remote's reach is fixed by the direction
    of its buttons, not their number. 10 steps: the sealed bet, one button,
    remote A, Nasib's remote B, the cheap messy remote C, the paint that names
    span, the door rule (DoorPick, an animated pick, under a story figure), Your turn, a
    visual Try it (pick the picture of a two-button remote's span), and the
    end. The toy shop is acted out in story scenes (the shopkeeper chacha,
    Nasib's pick, the toggle, the back shelf). Each screen opens by reacting to
    the last; the explanation sits in <Then> and shows only once the task is
    done, one phone screen each, with a watch-only figure in every <Then>.

    Split on 2026-09-23: remote B's slot sum (SlotsSame) and the side quest
    "the presses, worked out instead of hunted" (PressesFirst, SolveWalk) are
    their own journey now, 5.1b, 05a2_weak_battery.mdx: the same evening, remote
    C's cheap battery, and the question of knowing the presses before pressing.
    This journey's shopkeeper plants that battery in RemoteBought.

    The screens live in components/interactive/remote-journey.tsx. The first
    of the "span, independence, basis" journeys (Article 5); it opens Module 2.
    The plan is in 05_journey_specs.md. */}

import { Journey, Step, Then, SideQuest } from "@/components/journey/journey";
import {
  MovingDay,
  RemoteShelf,
  ShelfFour,
  CheapRemote,
  OneButton,
  LineNamed,
  OldRemote,
  AlmirahBuild,
  TileCounts,
  NasibPicksB,
  TwinButtons,
  SameDirection,
  CheapC,
  MessyRemote,
  BackStep,
  ToggleShow,
  PaintReach,
  ThreeSpans,
  DoorEveryTime,
  DoorPick,
  DoorInside,
  BackShelf,
  YourRemotes,
  DeadButton,
  WallPick,
  WallLine,
  RemoteBought,
  Finale,
  RuleLost,
  HuntAhead,
} from "@/components/interactive/remote-journey";

<Journey title="Span · 5.1">

<Step>

ছুটি শেষ। আর ফাহিমের family-ও বাসা shift করছে।

নতুন flat এখনও ফাঁকা। Furniture-ও এখনও কিছু গোছানো হয় নি। কোথায় কী রাখবেন, তা ঠিক করার জন্য আম্মু chalk দিয়ে floor এর চার জায়গায় mark করলেন।

Pack করার সময় Shiku র remote টা ভেঙে গেছে।

নিচের toy shop এ 4 ধরনের remote পাওয়া যাচ্ছে। প্রত্যেকটারই দুটো করে button। কিন্তু ফাহিমের কাছে অত টাকা নেই। ও শুধু একটা remote-ই কিনতে পারবে। Shiku কে দিয়ে ও মালপত্র গোছাবে বলে ভাবছে। Door এর corner থেকে Shiku কে চারটা spot এই যেতে পারতে হবে।

নাসিব শুনে বলল, দুটো button দিয়েই সব জায়গায় যাওয়া যাবে, সমস্যা নেই।

Decision নাও তাড়াতাড়ি। মাগরিবের পর দোকান বন্ধ হয়ে যাবে।

<MovingDay story />

<RemoteShelf />

<Then>

4 টা remote-ই দেখতে almost same। সবগুলোতেই দুটো করে button। তাহলে আলাদা করা যায় কীভাবে? একমাত্র button এর ওপর কী লেখা আছে, সেটা দেখে।

Button এর ওপর কিছু number লেখা আছে। Shiku কোথায় যেতে পারবে আর কোথায় পারবে না? সেটা decide করবে এই number গুলোই।

<ShelfFour />

</Then>

</Step>

<Step>

দোকানের সবচেয়ে সস্তা remote টায় 1 টাই button আছে। তাতে লেখা $v = (1, 1)$।

ফাহিম remote টা হাতে নিলো। এটাই আগে use করে দেখি।

<CheapRemote story />

আরে বাহ! Button টা দেখি সামনে-পিছনেও আনা-নেওয়া করা যায়। Press করার সময় যদি অর্ধেক গিয়ে থেমে যাই, তাহলে সেটা half-press হিসেবে count হবে।

<OneButton />

<Then>

কী এসেছে? Shiku যেখানেই থামুক, dot গুলো সব বসলো একটা সোজা line এর উপর। 3.2 এর শরবতের কথা মনে আছে তো? একটা card কে যত দিয়েই গুণ করেন, ওর মাথা থাকে ওই একটা line এর উপরেই। এখানেও তাই। আর line টা গেছে ঠিক দরজার উপর দিয়ে। খাটের mark? ওই line এ নাই। তাই এই remote বাদ।

এক button দিয়ে যেখানে যেখানে যাওয়া যায়, সব মিলিয়ে ওই line টা। এর একটা নাম আছে। **span**. মানে নাগাল, button যতদূর পৌঁছায়। লিখলে $\text{span}\{v\}$.

<LineNamed />

</Then>

</Step>

<Step>

এবার remote A, পুরানো ধরনের। একটা button এক tile east এ যায়, $e_1 = (1, 0)$। আরেকটা এক tile north এ, $e_2 = (0, 1)$. 3.3 এ Shiku র remote ঠিক এটাই ছিল।

<OldRemote />

<Then>

খেয়াল করেছেন? প্রতিটা mark এ যেতে দুইটা সংখ্যা press করলেন। ওগুলো কী? mark এর নিজের দুইটা সংখ্যা। আলমারি $(3, 5)$ এ। তাই $e_1$ তিনবার, $e_2$ পাঁচবার।

<AlmirahBuild />

Floor এর যেকোনো tile এর নাম বলুন। ওই tile এর দুইটা সংখ্যাই press এর count। তাই remote A র span পুরা floor.

নাসিবের rule আপাতত টিকে গেলো।

<TileCounts />

</Then>

</Step>

<Step>

নাসিব দোকানদার চাচাকে দিয়ে remote B নামিয়ে আনলো। দুইটা button: $u = (1, 1)$ আর $v = (2, 2)$। নাসিব হালকা গলায় বলল, দুইটা আলাদা জোড়া। এটাও সব জায়গায় যাবে।

<NasibPicksB story />

<TwinButtons />

<Then>

টেবিল তো সহজেই হয়ে গেলো। আলমারি? যতবার খুশি try করেন। Shiku গিয়ে পড়ে ওই একটা বাঁকা line এর উপরেই।

কাহিনী কী? button দুইটার দিকে তাকান। $(2, 2)$ হলো $(1, 1)$ এর দুইগুণ। তাই v একবার চাপা মানে u দুইবার চাপা। Button দুইটা। direction একটাই।

**নাসিবের rule প্রথমবার হারলো।**

<SameDirection />

</Then>

</Step>

<Step>

Shelf এর তিন নম্বর remote, C। এর button দুইটা দেখতে সবচেয়ে এলোমেলো: $u = (1, 2)$ আর $v = (2, 5)$।

দোকানদার চাচা remote টা নামাতে নামাতে বললেন, দাম কম এইটার। সংখ্যা দেইখা কেউ নেয় না।

<CheapC story />

B র মতো এটাও কি আটকে যাবে?

<MessyRemote />

<Then>

প্রথমে u পাঁচবার, Shiku উঠে গেলো $(5, 10)$ এ। আলমারি পড়ে রইলো অনেক নিচে। তারপর v একবার, উল্টা দিকে: $(5, 10) - (2, 5) = (3, 5)$। ঠিক আলমারির tile এ।

দেখতে এলোমেলো। কিন্তু u আর v ঠেলে দুই দিকে। একটা আরেকটার দুইগুণ, তিনগুণ? এরকম কিছু না। এটুকুই লাগে। একটু বেশি চলে যান। তারপর পিছিয়ে আসেন। নামা যায় যেকোনো tile এ।

<BackStep />

</Then>

</Step>

<Step>

চারটার তিনটা শেষ, মাগরিবও কাছে চলে এসেছে। দোকানদার চাচা বললেন, সব remote এ একটা toggle আছে। টিপ দিলে floor এ রং জ্বলবো। যেইখানে যেইখানে যাইতে পারে, সবখানে।

<ToggleShow story />

<PaintReach />

<Then>

একটা remote এর button গুলো যত রকমভাবে চাপা যায়, সবগুলো জায়গা একসাথে করুন। ওটাই ওই button গুলোর **span**। দুইটা button হলে লিখি $\text{span}\{u, v\}$.

রং কী দেখালো? span কখনো একটা line। কখনো পুরা floor। কোনটা হবে, ঠিক করে কে? button কয়টা, তা না। button গুলো কোন দিকে ঠেলে, সেটা।

<ThreeSpans />

</Then>

</Step>

<Step>

কিন্তু চারটা রং করা ছবিতেই একটা জিনিস এক। দরজার corner টা প্রতিবার রং এর ভিতরে পড়েছে।

<DoorEveryTime story />

<DoorPick />

<Then>

কেন? সব button শূন্যবার চাপা। এটাও একটা combination, $0 \cdot u + 0 \cdot v = (0, 0)$। ওটাই দরজা। তাই button যা-ই হোক, দরজা কখনো span এর বাইরে পড়ে না।

তাই span কখনো খালি না। Remote যত খারাপই হোক, অন্তত একটা জায়গায় যায়। নিজের দরজায়।

<DoorInside />

</Then>

</Step>

<Step>

দোকানদার চাচা পেছনের তাক থেকে আরো চারটা remote নিয়ে আসলেন। বললেন, রং ছাড়া কইতে পারবা?

<BackShelf story />

এবার আপনার পালা। রং করার আগেই বলুন। কোনটার span একটা line? আর কোনটার পুরা floor?

<YourRemotes />

<Then>

Remote 3 এ একটা $(0, 0)$ button ছিল। টিপলে Shiku এক চুলও নড়ে না। তাই ওটা থাকা আর না থাকা একই কথা। হাতে থাকলো একটা button, মানে একটা line।

<DeadButton />

দেখলেন? Button গুনে কিছুই বোঝা যায় না। দুইটা button দিয়ে line-ও হয়। আবার পুরা floor-ও হয়।

</Then>

</Step>

<Step>

## Try it

একটা remote এ শুধু $(2, 0)$ button। আরেকটায় দুইটাই, $(2, 0)$ আর $(-5, 0)$। দুইটার span কি একই?

<WallPick />

<Then>

দুইটার span একই। কেন? $(-5, 0)$ হলো $(2, 0)$ এর $-2.5$ গুণ। তাই এটাও যায় ওই একই দেয়াল বরাবর। শুধু উল্টা দিকে। আর উল্টা দিকে? প্রথম button টা minus এ চেপেই তো যাওয়া যেত।

“পুরা floor” বেছেছেন? তাহলে button গুনেছেন। Button আসলেই দুইটা। কিন্তু direction একটাই। তাই দুইটা button থাকলেও span সবসময় পুরা floor না।

<WallLine />

</Then>

</Step>

<Step>

## The end! ফাহিম কিনলো remote C

মাগরিবের ঠিক আগে ফাহিম দোকান থেকে বের হলো। হাতে সেই এলোমেলো দেখতে remote C। A দিয়েও কাজ হতো, কিন্তু C র দাম কম।

দোকানদার চাচা remote টা দিতে দিতে বললেন, সস্তা জিনিস। Battery-ও সস্তা। বেশি টিপাটিপি কইরো না।

<RemoteBought story />

চারটা remote এর result একটা একটা করে খুলুন, আর আপনার বাজির সাথে মিলিয়ে দেখুন।

<Finale />

<Then>

নাসিবের rule দুইবার হারলো। B আর D তে দুটো করে button। তবুও দুইটাই আটকে গেলো একটা line এ। তাহলে কোন rule টিকলো? **button গুনে লাভ নাই; দেখুন কতদূর যায়।** আর “কতদূর যায়” এর নামই span।

<RuleLost />

কিন্তু ফাহিম আলমারি খুঁজে পেয়েছিল টিপে টিপে। সস্তা battery তে ওভাবে কতক্ষণ চলবে? টিপার আগেই কি বলা যায়, কোন button কয়বার?

<HuntAhead />

<SideQuest title="বইয়ে span যেভাবে লেখে">

<Table
  head={["যেটা দেখবেন", "যেভাবে বলবেন"]}
  rows={[
    ["span{v}", "v এর span, একটা line (v শূন্য না হলে)"],
    ["span{u, v}", "u আর v মিলে যেখানে যেখানে যায়"],
    ["αu + βv", "u আর v এর একটা combination, 3.3 এর recipe"],
    ["span{v₁, …, vₙ}", "n টা vector মিলে যেখানে যেখানে যায়"],
    ["0 ∈ span{…}", "zero vector সবসময় span এর ভিতরে: দরজা"],
  ]}
/>

</SideQuest>

<LessonLink href="/dashboard/articles/math_for_ai/05a2_weak_battery" eyebrow="পরের lesson" title="সস্তা battery, টিপার আগেই হিসাব">
  একই সন্ধ্যা। ফ্রিজের নতুন mark, battery প্রায় শেষ। Button না চেপে press গুলো বের করা যায় কি না।
</LessonLink>

</Then>

</Step>

</Journey>

export const metadata = {
  title: "What is a Large Language Model?",
  description: "What an LLM is, what it is trained on, and what it can and cannot do.",
  type: "article",
};

# What is a Large Language Model?

TODO — one paragraph framing: a model that predicts the next piece of text, at a scale
where that turns into something that looks like understanding.

## What "large" actually means

TODO — parameters, training data, compute. Why scale changed the results.

## How it learns from text

TODO — next-token prediction over a huge corpus; no labels, no rules written by hand.

## What it is good at

TODO — summarising, rewriting, translating, drafting, answering from context.

## Where it breaks down

TODO — knowledge cutoff, hallucination, no access to private data, no citations.

export const metadata = { title: "Security Is Everyone’s Job", description: "User security awareness and practices — threats, passwords, and the habits that hold.", type: "slides" };

{/* Ported from the standalone reveal.js deck (misl/user-security-awareness/index.html).
    The content is the source deck; the design follows agentic-ai.mdx — same token
    system, type scale, layout vocabulary (.chapter/.evo/.duo/.panel/.cards/.flow/.pull)
    and per-chapter hue. Stock emoji are replaced by the line-art SVGs below, drawn in
    the same idiom as agentic-ai's: 200x150 viewBox, the f- and s- fill/stroke classes,
    accent-aware so each chapter's hue recolors the art for free.
    Delivery changes: the CDN reveal + notes plugin are dropped (deck.tsx owns those),
    the inline <script> became <Quiz> and <PasswordMeter />, the two vertical stacks are
    flattened (live sync only carries the horizontal index), Poll/Quiz/DragDrop slides
    were added so the deck can be scored, and the regulated-data slide is localized. */}

export const SVGS = {
  globe: `<svg viewBox="0 0 200 150" role="img" aria-label="An attack crosses the globe to reach one laptop">
    <circle class="f-surface s-line" cx="44" cy="72" r="30" stroke-width="1"/>
    <ellipse class="s-line" cx="44" cy="72" rx="12" ry="30" stroke-width="0.7" fill="none"/>
    <path class="s-line" d="M14 72 H74" stroke-width="0.7"/>
    <path class="s-line" d="M19 56 H69" stroke-width="0.7"/>
    <path class="s-line" d="M19 88 H69" stroke-width="0.7"/>
    <circle class="f-accent" cx="33" cy="58" r="3"/>
    <path class="s-accent" d="M42 46 Q98 8 146 44" stroke-width="1.1" fill="none" stroke-dasharray="4 3" marker-end="url(#gl-a)"/>
    <rect class="f-surface s-accent" x="126" y="60" width="54" height="34" rx="3" stroke-width="1.1"/>
    <path class="s-line" d="M120 97 H186" stroke-width="1.6"/>
    <text class="lbl" x="153" y="118" text-anchor="middle">YOU</text>
    <defs><marker id="gl-a" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto"><path class="f-accent" d="M0 0 L6 3 L0 6 Z"/></marker></defs>
  </svg>`,
  door: `<svg viewBox="0 0 200 150" role="img" aria-label="A bolted door beside a door held open from the inside">
    <rect class="f-surface s-line" x="18" y="26" width="60" height="98" rx="3" stroke-width="1"/>
    <rect class="f-surface s-accent" x="52" y="66" width="22" height="12" rx="2" stroke-width="1.1"/>
    <path class="s-accent" d="M74 72 H86" stroke-width="1.4"/>
    <circle class="s-line" cx="46" cy="72" r="3" stroke-width="0.9" fill="none"/>
    <text class="lbl" x="48" y="140" text-anchor="middle">BOLTED</text>
    <rect class="s-line" x="122" y="26" width="60" height="98" rx="3" stroke-width="1" fill="none"/>
    <path class="f-surface s-line" d="M122 26 L162 40 V110 L122 124 Z" stroke-width="1"/>
    <path class="s-accent" d="M186 96 Q168 96 156 84" stroke-width="1.1" fill="none" marker-end="url(#dr-a)"/>
    <circle class="f-accent" cx="150" cy="76" r="2.6"/>
    <text class="lbl" x="152" y="140" text-anchor="middle">OPENED</text>
    <defs><marker id="dr-a" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto"><path class="f-accent" d="M0 0 L6 3 L0 6 Z"/></marker></defs>
  </svg>`,
  virus: `<svg viewBox="0 0 200 150" role="img" aria-label="Code attached to a host program copies itself into two more files">
    <rect class="f-surface s-accent" x="16" y="48" width="48" height="58" rx="3" stroke-width="1.1"/>
    <path class="s-accent" d="M52 48 V60 H64" stroke-width="1.1" fill="none"/>
    <line class="s-line" x1="24" y1="70" x2="52" y2="70" stroke-width="0.7"/>
    <line class="s-line" x1="24" y1="78" x2="46" y2="78" stroke-width="0.7"/>
    <circle class="f-accent" cx="52" cy="94" r="6"/>
    <path class="s-accent" d="M52 84 V104 M42 94 H62 M45 87 L59 101 M59 87 L45 101" stroke-width="0.9"/>
    <text class="lbl" x="40" y="122" text-anchor="middle">HOST</text>
    <path class="s-accent" d="M70 76 Q96 50 122 46" stroke-width="1.1" fill="none" marker-end="url(#vi-a)"/>
    <path class="s-accent" d="M70 84 Q96 110 122 114" stroke-width="1.1" fill="none" marker-end="url(#vi-a)"/>
    <rect class="f-surface s-line" x="128" y="26" width="42" height="44" rx="3" stroke-width="0.9"/>
    <circle class="f-accent" cx="149" cy="48" r="4"/>
    <rect class="f-surface s-line" x="128" y="94" width="42" height="44" rx="3" stroke-width="0.9"/>
    <circle class="f-accent" cx="149" cy="116" r="4"/>
    <defs><marker id="vi-a" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto"><path class="f-accent" d="M0 0 L6 3 L0 6 Z"/></marker></defs>
  </svg>`,
  worm: `<svg viewBox="0 0 200 150" role="img" aria-label="One infected machine spreads itself across every machine on the network">
    <path class="s-line" d="M40 40 H160 M40 110 H160 M40 40 V110 M160 40 V110 M40 40 L160 110 M160 40 L40 110" stroke-width="0.55"/>
    <circle class="f-accent" cx="40" cy="40" r="9"/>
    <circle class="f-surface s-accent" cx="160" cy="40" r="9" stroke-width="1.1"/>
    <circle class="f-surface s-accent" cx="40" cy="110" r="9" stroke-width="1.1"/>
    <circle class="f-surface s-line" cx="160" cy="110" r="9" stroke-width="0.9"/>
    <circle class="f-surface s-line" cx="100" cy="75" r="9" stroke-width="0.9"/>
    <path class="s-accent" d="M50 44 H84" stroke-width="1.2" marker-end="url(#wo-a)"/>
    <path class="s-accent" d="M46 50 V96" stroke-width="1.2" marker-end="url(#wo-a)"/>
    <path class="s-accent" d="M52 34 H144" stroke-width="1.2" marker-end="url(#wo-a)"/>
    <text class="lbl" x="40" y="24" text-anchor="middle">PATIENT ZERO</text>
    <defs><marker id="wo-a" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto"><path class="f-accent" d="M0 0 L6 3 L0 6 Z"/></marker></defs>
  </svg>`,
  trojan: `<svg viewBox="0 0 200 150" role="img" aria-label="A gift box whose contents quietly leave through a back door">
    <rect class="f-surface s-accent" x="42" y="58" width="86" height="60" rx="3" stroke-width="1.1"/>
    <rect class="f-surface s-accent" x="34" y="44" width="102" height="18" rx="3" stroke-width="1.1"/>
    <path class="s-accent" d="M85 44 V70" stroke-width="1"/>
    <path class="s-accent" d="M85 44 Q70 26 60 36 Q56 44 76 44 M85 44 Q100 26 110 36 Q114 44 94 44" stroke-width="1" fill="none"/>
    <circle class="f-surface s-accent" cx="58" cy="92" r="7" stroke-width="1"/>
    <circle class="s-accent" cx="58" cy="92" r="2.4" stroke-width="0.8" fill="none"/>
    <path class="s-accent" d="M65 92 H108 M100 92 V99 M108 92 V100" stroke-width="1"/>
    <path class="s-accent" d="M128 92 H176" stroke-width="1.1" stroke-dasharray="4 3" marker-end="url(#tr-a)"/>
    <text class="lbl" x="134" y="80" text-anchor="start">OUT THE BACK</text>
    <text class="lbl" x="86" y="140" text-anchor="middle">LOOKS LIKE A GIFT</text>
    <defs><marker id="tr-a" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto"><path class="f-accent" d="M0 0 L6 3 L0 6 Z"/></marker></defs>
  </svg>`,
  social: `<svg viewBox="0 0 200 150" role="img" aria-label="A caller asks politely and the key is handed over">
    <path class="f-surface s-accent" d="M26 44 q6 -8 14 -2 l8 8 q4 4 0 8 l-6 6 q6 14 20 20 l6 -6 q4 -4 8 0 l8 8 q6 8 -2 14 q-14 10 -34 -8 q-20 -18 -22 -48 Z" stroke-width="1.1"/>
    <path class="f-surface s-accent" d="M96 34 h84 q6 0 6 6 v40 q0 6 -6 6 h-56 l-14 14 v-14 h-14 q-6 0 -6 -6 v-40 q0 -6 6 -6 Z" stroke-width="1.1"/>
    <circle class="s-accent" cx="122" cy="60" r="7" stroke-width="1.1" fill="none"/>
    <path class="s-accent" d="M129 60 H156 M150 60 V68 M156 60 V70" stroke-width="1.1"/>
    <text class="lbl" x="140" y="112" text-anchor="middle">JUST A QUICK CHECK</text>
    <path class="s-line" d="M74 78 H92" stroke-width="0.7" stroke-dasharray="3 3"/>
  </svg>`,
  rootkit: `<svg viewBox="0 0 200 150" role="img" aria-label="A back door hidden beneath the operating system, with the alarm disabled">
    <rect class="f-surface s-line" x="24" y="28" width="152" height="22" rx="3" stroke-width="0.9"/>
    <text class="lbl" x="100" y="43" text-anchor="middle">WHAT YOU SEE</text>
    <rect class="f-surface s-line" x="24" y="58" width="152" height="22" rx="3" stroke-width="0.9"/>
    <text class="lbl" x="100" y="73" text-anchor="middle">OPERATING SYSTEM</text>
    <rect class="f-surface s-accent" x="24" y="88" width="152" height="34" rx="3" stroke-width="1.1" stroke-dasharray="5 3"/>
    <rect class="f-surface s-accent" x="36" y="96" width="20" height="26" rx="2" stroke-width="1.1"/>
    <circle class="f-accent" cx="51" cy="110" r="1.8"/>
    <text class="lbl" x="112" y="112" text-anchor="middle">PERMANENT BACK DOOR</text>
    <circle class="s-line" cx="164" cy="39" r="7" stroke-width="0.9" fill="none"/>
    <circle class="f-line" cx="164" cy="39" r="2.4"/>
    <path class="s-accent" d="M157 46 L171 32" stroke-width="1.4"/>
  </svg>`,
  botnet: `<svg viewBox="0 0 200 150" role="img" aria-label="One attacker drives a handler that commands thousands of hijacked machines">
    <rect class="f-surface s-accent" x="12" y="60" width="34" height="28" rx="4" stroke-width="1.1"/>
    <text class="lbl-ink" x="29" y="78" text-anchor="middle">ATK</text>
    <path class="s-accent" d="M50 74 H74" stroke-width="1.1" marker-end="url(#bo-a)"/>
    <rect class="f-surface s-accent" x="78" y="56" width="40" height="36" rx="4" stroke-width="1.1"/>
    <text class="lbl-ink" x="98" y="78" text-anchor="middle">C2</text>
    <path class="s-accent" d="M120 66 L152 30" stroke-width="1" marker-end="url(#bo-a)"/>
    <path class="s-accent" d="M120 71 L152 56" stroke-width="1" marker-end="url(#bo-a)"/>
    <path class="s-accent" d="M120 78 L152 92" stroke-width="1" marker-end="url(#bo-a)"/>
    <path class="s-accent" d="M120 83 L152 118" stroke-width="1" marker-end="url(#bo-a)"/>
    <rect class="f-surface s-line" x="156" y="20" width="30" height="20" rx="3" stroke-width="0.9"/>
    <rect class="f-surface s-line" x="156" y="48" width="30" height="20" rx="3" stroke-width="0.9"/>
    <rect class="f-surface s-accent" x="156" y="82" width="30" height="20" rx="3" stroke-width="1.1"/>
    <rect class="f-surface s-line" x="156" y="110" width="30" height="20" rx="3" stroke-width="0.9"/>
    <text class="lbl" x="171" y="97" text-anchor="middle">YOU</text>
    <defs><marker id="bo-a" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto"><path class="f-accent" d="M0 0 L6 3 L0 6 Z"/></marker></defs>
  </svg>`,
  mitm: `<svg viewBox="0 0 200 150" role="img" aria-label="Traffic passes through an attacker who reads everything in transit">
    <rect class="f-surface s-line" x="8" y="58" width="40" height="32" rx="4" stroke-width="0.9"/>
    <text class="lbl" x="28" y="78" text-anchor="middle">YOU</text>
    <rect class="f-surface s-accent" x="76" y="52" width="48" height="44" rx="4" stroke-width="1.1"/>
    <path class="s-accent" d="M88 74 q12 -11 24 0 q-12 11 -24 0 Z" stroke-width="1" fill="none"/>
    <circle class="f-accent" cx="100" cy="74" r="3"/>
    <text class="lbl" x="100" y="110" text-anchor="middle">FREE WIFI</text>
    <ellipse class="f-surface s-line" cx="172" cy="56" rx="18" ry="6" stroke-width="0.9"/>
    <path class="s-line" d="M154 56 V88 a18 6 0 0 0 36 0 V56" stroke-width="0.9" fill="none"/>
    <path class="s-accent" d="M52 68 H72" stroke-width="1.1" marker-end="url(#mi-a)"/>
    <path class="s-accent" d="M128 68 H150" stroke-width="1.1" marker-end="url(#mi-a)"/>
    <path class="s-line" d="M150 82 H128" stroke-width="0.9" marker-end="url(#mi-l)"/>
    <path class="s-line" d="M72 82 H52" stroke-width="0.9" marker-end="url(#mi-l)"/>
    <defs>
      <marker id="mi-a" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto"><path class="f-accent" d="M0 0 L6 3 L0 6 Z"/></marker>
      <marker id="mi-l" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto"><path class="f-line" d="M0 0 L6 3 L0 6 Z"/></marker>
    </defs>
  </svg>`,
  phish: `<svg viewBox="0 0 200 150" role="img" aria-label="A hook lifts a credential card out of an email">
    <rect class="f-surface s-line" x="20" y="70" width="100" height="60" rx="3" stroke-width="1"/>
    <path class="s-line" d="M20 70 L70 106 L120 70" stroke-width="1" fill="none"/>
    <rect class="f-surface s-accent" x="82" y="34" width="66" height="40" rx="4" stroke-width="1.1"/>
    <line class="s-accent" x1="90" y1="48" x2="140" y2="48" stroke-width="0.9"/>
    <line class="s-line" x1="90" y1="58" x2="124" y2="58" stroke-width="0.7"/>
    <line class="s-line" x1="90" y1="66" x2="112" y2="66" stroke-width="0.7"/>
    <text class="lbl" x="164" y="120" text-anchor="middle">BAIT</text>
    <path class="s-accent" d="M170 8 V70 q0 18 -16 18 q-14 0 -14 -12 q0 -8 8 -8" stroke-width="1.4" fill="none"/>
  </svg>`,
  layers: `<svg viewBox="0 0 200 150" role="img" aria-label="Four nested rings of defence with the person at the centre">
    <rect class="s-line" x="10" y="12" width="180" height="126" rx="10" stroke-width="1" fill="none"/>
    <text class="lbl" x="100" y="26" text-anchor="middle">ROUTER FIREWALL</text>
    <rect class="s-line" x="26" y="30" width="148" height="90" rx="9" stroke-width="1" fill="none"/>
    <text class="lbl" x="100" y="44" text-anchor="middle">OS FIREWALL · PATCHES</text>
    <rect class="s-accent" x="42" y="48" width="116" height="54" rx="8" stroke-width="1.1" fill="none"/>
    <text class="lbl" x="100" y="62" text-anchor="middle">ANTIVIRUS · PRIVILEGE</text>
    <rect class="f-surface s-accent" x="62" y="68" width="76" height="26" rx="6" stroke-width="1.4"/>
    <text class="lbl-ink" x="100" y="85" text-anchor="middle">YOU</text>
    <path class="s-accent" d="M100 130 V108" stroke-width="1.1" marker-end="url(#la-a)"/>
    <defs><marker id="la-a" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto"><path class="f-accent" d="M0 0 L6 3 L0 6 Z"/></marker></defs>
  </svg>`,
  backup: `<svg viewBox="0 0 200 150" role="img" aria-label="Live data copied to an off-site store and the restore verified">
    <ellipse class="f-surface s-accent" cx="44" cy="46" rx="26" ry="8" stroke-width="1.1"/>
    <path class="s-accent" d="M18 46 V96 a26 8 0 0 0 52 0 V46" stroke-width="1.1" fill="none"/>
    <line class="s-line" x1="18" y1="70" x2="70" y2="70" stroke-width="0.7"/>
    <text class="lbl" x="44" y="122" text-anchor="middle">LIVE</text>
    <path class="s-accent" d="M76 62 H122" stroke-width="1.1" marker-end="url(#ba-a)"/>
    <text class="lbl" x="99" y="54" text-anchor="middle">COPY</text>
    <ellipse class="f-surface s-line" cx="156" cy="46" rx="26" ry="8" stroke-width="1" stroke-dasharray="4 3"/>
    <path class="s-line" d="M130 46 V96 a26 8 0 0 0 52 0 V46" stroke-width="1" fill="none" stroke-dasharray="4 3"/>
    <text class="lbl" x="156" y="122" text-anchor="middle">OFF-SITE</text>
    <path class="s-accent" d="M122 88 H78" stroke-width="1.1" stroke-dasharray="3 3" marker-end="url(#ba-a)"/>
    <text class="lbl" x="99" y="104" text-anchor="middle">RESTORE</text>
    <defs><marker id="ba-a" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto"><path class="f-accent" d="M0 0 L6 3 L0 6 Z"/></marker></defs>
  </svg>`,
  keylen: `<svg viewBox="0 0 200 150" role="img" aria-label="A short key snaps where a long key holds">
    <circle class="f-surface s-line" cx="26" cy="36" r="12" stroke-width="1"/>
    <circle class="s-line" cx="26" cy="36" r="4" stroke-width="0.9" fill="none"/>
    <path class="s-line" d="M38 36 H92" stroke-width="1.4"/>
    <path class="s-line" d="M78 36 V46 M88 36 V46" stroke-width="1.2"/>
    <path class="s-accent" d="M106 28 L120 44 M120 28 L106 44" stroke-width="1.6"/>
    <text class="lbl" x="100" y="68" text-anchor="middle">8 CHARS · 2 HOURS</text>
    <circle class="f-surface s-accent" cx="26" cy="100" r="12" stroke-width="1.1"/>
    <circle class="s-accent" cx="26" cy="100" r="4" stroke-width="1" fill="none"/>
    <path class="s-accent" d="M38 100 H164" stroke-width="1.6"/>
    <path class="s-accent" d="M136 100 V112 M146 100 V112 M156 100 V112" stroke-width="1.3"/>
    <text class="lbl" x="100" y="132" text-anchor="middle">16 CHARS · CENTURIES</text>
  </svg>`,
  speak: `<svg viewBox="0 0 200 150" role="img" aria-label="One coworker speaks up and the scheme is found">
    <path class="f-surface s-line" d="M18 34 h70 q6 0 6 6 v34 q0 6 -6 6 h-46 l-14 12 v-12 h-10 q-6 0 -6 -6 v-34 q0 -6 6 -6 Z" stroke-width="0.9"/>
    <line class="s-line" x1="30" y1="50" x2="78" y2="50" stroke-width="0.7"/>
    <line class="s-line" x1="30" y1="62" x2="66" y2="62" stroke-width="0.7"/>
    <path class="f-surface s-accent" d="M104 62 h70 q6 0 6 6 v34 q0 6 -6 6 h-10 v12 l-14 -12 h-46 q-6 0 -6 -6 v-34 q0 -6 6 -6 Z" stroke-width="1.3"/>
    <path class="s-accent" d="M139 76 V90" stroke-width="1.6"/>
    <circle class="f-accent" cx="139" cy="97" r="2.2"/>
    <text class="lbl" x="139" y="140" text-anchor="middle">SOMEONE SAID SOMETHING</text>
  </svg>`,
};

<link href="https://fonts.googleapis.com/css2?family=Hanken+Grotesk:ital,wght@0,400;0,500;0,600;0,700;1,400&family=Space+Grotesk:wght@600;700&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet" />

<style>{`
/* Token system, type scale and primitives are agentic-ai.mdx's — same em-based scale
   off .reveal's own font-size (never html{font-size}, which leaks to the dashboard).
   New here: .act (live activities), .pwm-* (PasswordMeter), .mail (the phishing mock),
   .lanes (the fraud chart) and .stats. */

.reveal, .reveal-viewport {
  --hue: 275;                          /* default: title + close */
  --bg:      oklch(0.962 0.028 var(--hue));
  --bg-2:    oklch(0.928 0.038 var(--hue));
  --surface: oklch(0.99 0.012 var(--hue));
  --line:    oklch(0.85 0.045 var(--hue));
  --ink:     oklch(0.22 0.03 var(--hue));
  --muted:   oklch(0.42 0.035 var(--hue));
  --faint:   oklch(0.50 0.035 var(--hue));
  --accent:  oklch(0.56 0.21 var(--hue));
  --accent-ink: oklch(0.45 0.19 var(--hue));
  --accent-dim: oklch(0.56 0.21 var(--hue) / 0.16);
  --accent-line: oklch(0.56 0.21 var(--hue) / 0.48);
  --accent-glow: oklch(0.56 0.21 var(--hue) / 0.26);
  --danger:  oklch(0.50 0.17 32);
  --caution: oklch(0.58 0.14 75);
  --safe:    oklch(0.52 0.14 150);
  --elev: 0 1px 2px oklch(0.22 0.02 var(--hue) / 0.06),
          0 6px 20px oklch(0.22 0.02 var(--hue) / 0.07);
  --display: "Space Grotesk", system-ui, sans-serif;
  --sans: "Hanken Grotesk", system-ui, sans-serif;
  --mono: "JetBrains Mono", ui-monospace, monospace;
  --t-display: 4.4em;
  --t-quote:   2.4em;
  --t-h2:      2em;
  --t-h3:      1.25em;
  --t-lead:    1.05em;
  --t-body:    0.95em;
  --t-small:   0.78em;
  --t-label:   0.62em;
  --s1: 0.35em;
  --s2: 0.6em;
  --s3: 1em;
  --s4: 1.5em;
  --s5: 2.2em;
  --s6: 3.2em;
}

/* Per-chapter hue. The descendant form recolors a slide's content; the compound
   form recolors the deck background (deck.tsx mirrors the current slide's ch-*
   class onto .reveal, because custom props only cascade down). */
.reveal.ch-cost,   .reveal .ch-cost   { --hue: 25;  }
.reveal.ch-threat, .reveal .ch-threat { --hue: 300; }
.reveal.ch-decep,  .reveal .ch-decep  { --hue: 60;  }
.reveal.ch-pass,   .reveal .ch-pass   { --hue: 145; }
.reveal.ch-owned,  .reveal .ch-owned  { --hue: 195; }
.reveal.ch-defend, .reveal .ch-defend { --hue: 245; }
.reveal.ch-gov,    .reveal .ch-gov    { --hue: 330; }

.reveal, .reveal-viewport {
  background:
    radial-gradient(90% 70% at 100% 0%, var(--accent-glow), transparent 60%),
    radial-gradient(80% 80% at 0% 100%, oklch(0.99 0.02 var(--hue) / 0.55), transparent 55%),
    linear-gradient(155deg, var(--bg) 30%, var(--bg-2) 100%);
  transition: background-color 600ms ease;
}
.reveal *, .reveal *::before, .reveal *::after { box-sizing: border-box; }

.reveal {
  font-family: var(--sans);
  font-size: 20px;
  font-weight: 400;
  color: var(--ink);
  -webkit-font-smoothing: antialiased;
  font-kerning: normal;

  /* App tokens re-scoped so Quiz/Poll/DragDrop follow the deck palette. They are
     Tailwind token classes (text-foreground, border-border, bg-accent…) mapped to
     these vars by @theme inline in globals.css. --muted/--surface/--accent/--danger
     are already deck tokens above and double as the app's. */
  --background: var(--bg);
  --foreground: var(--ink);
  --border: var(--line);
  --accent-text: var(--accent-ink);
  --accent-foreground: oklch(0.99 0.012 var(--hue));
  --accent-bright: oklch(0.66 0.19 var(--hue));
  --accent-deep: oklch(0.42 0.17 var(--hue));
}
.reveal .slides { text-align: left; }

/* Type */
:where(.reveal) :where(h1, h2, h3, h4, p, ul, ol, li, blockquote, table) { margin: 0; }
.reveal h1, .reveal h2, .reveal h3, .reveal h4 {
  font-family: var(--sans);
  color: var(--ink);
  font-weight: 600;
  text-transform: none;
  text-wrap: balance;
}
.reveal h1 {
  font-family: var(--display);
  font-size: var(--t-display); font-weight: 700;
  line-height: 1; letter-spacing: -0.03em;
}
.reveal h2 { font-size: var(--t-h2); line-height: 1.12; letter-spacing: -0.025em; }
.reveal h3 { font-size: var(--t-h3); line-height: 1.25; }
.reveal h4 { font-size: var(--t-body); line-height: 1.3; }
.reveal p, .reveal li { line-height: 1.6; }
.reveal p  { color: var(--ink); text-wrap: pretty; max-width: 62ch; }
.reveal strong { color: var(--ink); font-weight: 600; }
.reveal em { color: var(--accent-ink); font-style: italic; }
.reveal a  { color: var(--accent-ink); }
.reveal li { max-width: 62ch; text-wrap: pretty; }
.reveal li + li { margin-top: var(--s2); }
.reveal ::selection { background: var(--accent); color: var(--bg); }

.reveal .lead { color: var(--muted); font-size: var(--t-lead); max-width: 44ch; line-height: 1.55; }
.reveal .fine { color: var(--faint); font-size: var(--t-small); }
.reveal .dim  { color: var(--faint); }
.mono { font-family: var(--mono); font-size: 0.9em; color: var(--accent-ink);
        background: var(--accent-dim); border-radius: 0.25em; padding: 0.1em 0.4em; }

.reveal .slides section > *:not(section) + *:not(section),
.title-slide > * + *,
.pull > * + * { margin-top: var(--s4); }
.reveal .slides section > h2 + .lead,
.reveal .slides section > h2 + p,
.reveal h3 + p { margin-top: var(--s3); }
.reveal .slides section > h2 + .cards,
.reveal .slides section > h2 + .duo,
.reveal .slides section > h2 + .lanes,
.reveal .slides section > h2 + .stats,
.reveal .slides section > h2 + table,
.reveal .slides section > h2 + .flow { margin-top: var(--s5); }

/* Reveal chrome — this style tag renders after reveal's white.css, so it wins. */
.reveal .progress { color: var(--accent); height: 3px; }
.reveal .slide-number { background: transparent; color: var(--faint); font-family: var(--mono); font-size: 14px; }
.reveal .controls { color: var(--accent); }

/* Primitives */
.panel .tag,
.card .h,
.reveal thead th,
.pull cite {
  font-family: var(--sans);
  font-size: var(--t-label);
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  line-height: 1.3;
}

.duo { display: grid; grid-template-columns: 1fr 1fr; gap: var(--s4); align-items: stretch; }

.panel {
  position: relative; isolation: isolate; overflow: hidden;
  background: var(--surface); border-radius: 0.55em; padding: var(--s4);
  display: flex; flex-direction: column; gap: var(--s3);
  box-shadow: var(--elev);
}
.panel::before, .card::before {
  content: ""; position: absolute; inset: 0; z-index: -1; border-radius: inherit;
  background-image: radial-gradient(circle, var(--accent) 0.7px, transparent 0.7px);
  background-size: 0.72em 0.72em;
  opacity: 0.16;
  -webkit-mask-image: radial-gradient(115% 85% at 100% 0%, #000 0%, transparent 55%);
          mask-image: radial-gradient(115% 85% at 100% 0%, #000 0%, transparent 55%);
}
.panel::after, .card::after {
  content: ""; position: absolute; top: 0.85em; right: 0.85em; z-index: -1;
  width: 0.8em; height: 0.8em;
  border-top: 1.5px solid var(--accent); border-right: 1.5px solid var(--accent);
  opacity: 0.5;
}
.panel.accent { background: linear-gradient(180deg, var(--accent-dim), transparent 70%), var(--surface); }
.panel.accent::before, .card.is-accent::before { opacity: 0.22; }
.panel.accent::after,  .card.is-accent::after  { opacity: 0.7; }
.panel .tag { color: var(--faint); display: block; margin-bottom: calc(var(--s1) - var(--s3)); }
.panel.accent .tag { color: var(--accent-ink); }
.panel.danger .tag { color: var(--danger); }
.panel.danger::after { border-color: var(--danger); }
.panel .headline { font-size: var(--t-h3); color: var(--ink); font-weight: 500; line-height: 1.3; }
.panel .headline .sub { color: var(--muted); font-weight: 400; }
.panel .prose { color: var(--muted); font-size: var(--t-small); }
.panel ul, .card ul, .evo-copy ul { display: block; list-style: none; padding: 0; }
.panel li, .evo-copy li { padding-left: 1.5em; position: relative; color: var(--muted); }
.panel li::before, .evo-copy li::before, .card li::before {
  content: ""; position: absolute; left: 0; top: 0.58em;
  width: 0.42em; height: 0.42em;
  border: 1.5px solid var(--accent); border-radius: 1px;
  background: var(--surface); transform: rotate(45deg);
}
.panel li::after, .evo-copy li::after, .card li::after {
  content: ""; position: absolute; left: 0.52em; top: 0.78em;
  width: 0.5em; height: 1.5px; background: var(--accent-line);
  transform-origin: left center;
}
.reveal li.fragment::before { opacity: 0; transform: rotate(45deg) scale(0.35); }
.reveal li.fragment.visible::before {
  opacity: 1; transform: rotate(45deg) scale(1);
  transition: transform 220ms ease, opacity 220ms ease;
}
.reveal li.fragment::after { transform: scaleX(0); }
.reveal li.fragment.visible::after { transform: scaleX(1); transition: transform 220ms ease 60ms; }
.panel .verdict { margin-top: auto; }

.verdict {
  align-self: flex-start;
  display: inline-flex; align-items: center; gap: 0.5em;
  font-size: var(--t-small); font-weight: 500; color: var(--ink);
  background: var(--bg-2); border: 1px solid var(--line); border-radius: 999px;
  padding: 0.5em 1em;
}
.verdict.is-accent { border-color: var(--accent-line); }
.verdict.is-danger { border-color: var(--danger); }
.verdict .dot { width: 0.28em; height: 0.28em; border-radius: 50%; background: var(--accent); }
.verdict.is-danger .dot { background: var(--danger); }

.pull blockquote {
  border: none; background: none; box-shadow: none; margin: 0; padding: 0;
  font-family: var(--display); font-weight: 600;
  font-size: var(--t-quote); line-height: 1.14;
  letter-spacing: -0.02em; color: var(--ink); text-wrap: balance; max-width: 22ch;
}
.pull blockquote .hl { color: var(--accent-ink); }
.pull cite { display: block; margin-top: var(--s5); font-style: normal; color: var(--faint); }

.reveal .flow { list-style: none; padding: 0; margin: 0; counter-reset: step; }
.flow li { position: relative; padding: 0 var(--s4) var(--s3) 2.1em; max-width: none; counter-increment: step; }
.flow li + li { margin-top: 0; }
.flow li::before {
  content: counter(step, decimal-leading-zero);
  position: absolute; left: 0; top: 0.3em;
  font-family: var(--mono); font-size: var(--t-label); font-weight: 700;
  color: var(--accent);
}
.flow .t { display: block; font-weight: 500; color: var(--ink); font-size: var(--t-body); line-height: 1.35; }
.flow .d { display: block; color: var(--muted); font-size: var(--t-small); margin-top: 0.25em; }
.flow.cols {
  display: grid; grid-template-columns: 1fr 1fr;
  grid-template-rows: repeat(var(--rows, 4), auto);
  grid-auto-flow: column; column-gap: var(--s5); max-width: 40em;
}

/* Concept + its diagram */
.evo { display: grid; grid-template-columns: minmax(0, 22em) minmax(0, 1fr); gap: var(--s6); align-items: center; }
.evo.is-flip { grid-template-columns: minmax(0, 1fr) minmax(0, 22em); }
.evo.is-flip .evo-art { order: 2; }
.evo-copy > * + * { margin-top: var(--s3); }
.evo-copy .n { font-family: var(--mono); color: var(--accent);
               font-size: var(--t-label); font-weight: 700; letter-spacing: 0.1em; }
.evo-copy h2 { margin-top: var(--s1); }
.evo-art { display: flex; align-items: center; justify-content: center; }
.evo-art svg { width: 100%; max-width: 20em; height: auto; stroke-linecap: round; stroke-linejoin: round; }
.evo-art .s-accent { stroke: var(--accent); fill: none; }
.evo-art .s-line   { stroke: var(--line);   fill: none; }
.evo-art .f-surface{ fill: var(--surface); }
.evo-art .f-accent { fill: var(--accent); }
.evo-art .f-line   { fill: var(--line); }
.evo-art .lbl { font-family: var(--mono); fill: var(--faint);
                font-size: 7px; letter-spacing: 0.06em; text-transform: uppercase; }
.evo-art .lbl-ink { fill: var(--muted); font-family: var(--sans); font-size: 8px; font-weight: 600; }
.evo-art .glyph { fill: var(--accent-ink); font-family: var(--sans); font-size: 13px; font-weight: 700; }

/* Feature grid. Named .cards, not .grid: this style tag is not scoped to the deck's
   own markup, and Quiz/Poll/DragDrop use Tailwind's "grid" utility internally — a bare
   .grid rule here retargets their option badge and the countdown ring (the option
   letter lands mid-label). Never redefine a Tailwind utility name in an article. */
.cards { display: grid; grid-template-columns: repeat(auto-fit, minmax(var(--cards-min, 9em), 1fr)); gap: var(--s3); }
.cards.is-2up { grid-template-columns: repeat(2, 1fr); }
.cards.is-3up { grid-template-columns: repeat(3, 1fr); }
.cards.is-tight { gap: var(--s2); max-width: 52em; }
.cards.is-tight .card { padding: var(--s3); }
.cards.is-tight .card h3 { font-size: var(--t-body); }
.cards.is-tight .card p { font-size: var(--t-small); }
.cards.is-tight .card > * + * { margin-top: var(--s1); }
.card {
  position: relative; isolation: isolate; overflow: hidden;
  background: var(--surface); border-radius: 0.5em; padding: var(--s4);
  box-shadow: var(--elev);
}
.card > * + * { margin-top: var(--s3); }
.card .h + * { margin-top: var(--s1); }
.card .h { color: var(--accent-ink); display: flex; gap: 0.6em; align-items: baseline; }
.card .h .num { color: var(--faint); font-family: var(--mono); font-weight: 700; letter-spacing: 0; }
.card h3.is-danger { color: var(--danger); }
.card p { color: var(--muted); }
.card .note { color: var(--accent-ink); font-size: var(--t-small); font-weight: 500; }
.card li { color: var(--muted); font-size: var(--t-small); padding-left: 1.5em; position: relative; }
.card.is-accent { background: linear-gradient(180deg, var(--accent-dim), transparent 70%), var(--surface); }
.card.is-danger::after { border-color: var(--danger); opacity: 0.7; }
.card.is-danger .h { color: var(--danger); }

/* Table */
.reveal table { border-collapse: collapse; width: 100%; max-width: 46em; font-size: var(--t-body); }
.reveal thead th {
  color: var(--accent-ink); text-align: left;
  border-bottom: 1px solid var(--accent-line); padding: 0 0.9em 0.7em;
}
.reveal tbody td { padding: 0.62em 0.9em; border-bottom: 1px solid var(--line); color: var(--muted); vertical-align: top; }
.reveal tbody td:first-child { color: var(--ink); font-weight: 500; padding-left: 0; }
.reveal thead th:first-child { padding-left: 0; }
.reveal tbody tr:last-child td { border-bottom: none; }
.reveal td.num { font-family: var(--mono); font-size: var(--t-small); }
.reveal td.is-danger  { color: var(--danger); font-weight: 500; }
.reveal td.is-caution { color: var(--caution); font-weight: 500; }
.reveal td.is-safe    { color: var(--safe); font-weight: 500; }

/* Chips */
.reveal .risks, .reveal .chips {
  list-style: none; padding: 0; margin: 0; display: flex; flex-wrap: wrap; gap: var(--s2);
}
.risks li, .chips li {
  max-width: none; font-size: var(--t-small); font-weight: 500; color: var(--ink);
  border-radius: 999px; padding: 0.5em 1em;
}
.risks li { border: 1px solid var(--danger); background: oklch(0.50 0.17 32 / 0.08); }
.chips li { border: 1px solid var(--line); background: var(--surface); color: var(--muted); }
.risks li + li, .chips li + li { margin-top: 0; }

/* Quoted speech */
.quote-inline {
  border: none; box-shadow: none; background: none; padding: 0;
  font-style: italic; color: var(--muted); font-size: var(--t-lead); line-height: 1.5;
  max-width: 40ch; text-indent: -0.42em;
}
.quote-inline::before { content: "\\201C"; color: var(--accent); font-style: normal; }
.quote-inline::after  { content: "\\201D"; color: var(--accent); font-style: normal; }

.caption-strong { font-weight: 500; color: var(--accent-ink); font-size: var(--t-lead); }

/* Numbers */
.stats { display: grid; grid-template-columns: repeat(auto-fit, minmax(8em, 1fr)); gap: var(--s5) var(--s4); }
.stat .n { display: block; font-family: var(--display); font-size: 2.6em; font-weight: 700;
           color: var(--accent-ink); line-height: 1; letter-spacing: -0.03em; }
.stat .l { display: block; margin-top: var(--s2); color: var(--muted); font-size: var(--t-small); max-width: 16ch; }

/* Fraud-detection chart */
.lanes { display: grid; gap: var(--s2); max-width: 42em; }
.lane { display: grid; grid-template-columns: 9.5em 1fr 3em; align-items: center; gap: var(--s3);
        font-size: var(--t-small); color: var(--muted); }
.lane .track { height: 0.55em; background: var(--bg-2); border-radius: 999px; overflow: hidden; }
.lane .track > i { display: block; height: 100%; border-radius: 999px; background: var(--accent-line); }
.lane.is-top .track > i { background: var(--accent); }
.lane.is-top { color: var(--ink); font-weight: 500; }
.lane .v { font-family: var(--mono); color: var(--ink); text-align: right; }

/* The phishing mock — a document, not a screenshot */
.mail {
  background: var(--surface); border: 1px solid var(--line); border-radius: 0.5em;
  padding: var(--s4); font-size: var(--t-small); line-height: 1.65; color: var(--muted);
  box-shadow: var(--elev); max-width: 46em;
}
.mail .hdr {
  border-bottom: 1px solid var(--line); padding-bottom: var(--s2); margin-bottom: var(--s3);
  font-family: var(--mono); font-size: var(--t-label); color: var(--faint); letter-spacing: 0.04em;
}
.mail .hdr b { color: var(--muted); font-weight: 700; }
.mail p + p { margin-top: var(--s2); }
.mail p { max-width: none; color: var(--muted); }
.flag { color: var(--ink); background: oklch(0.50 0.17 32 / 0.10);
        box-shadow: inset 0 -1.5px 0 var(--danger); border-radius: 2px; padding: 0 0.15em; }

/* Live activities. Quiz/Poll/DragDrop style themselves with Tailwind
   (mt-6 flex flex-col gap-*), but reveal's white.css ".reveal ul" is specificity
   0,1,1 against a utility's 0,1,0 — so it wins and makes the option list an
   inline-block with a 1em indent and no top margin: the list shrink-wraps, the
   flex gap never applies, and mt-6 is dead. Undo exactly those four properties and
   let the components' own gap / flex-direction utilities take it from there. */
.act { max-width: 34em; margin: 0 auto; }
.reveal .act ul, .reveal .act ol {
  display: flex;
  margin: 1.5rem 0 0;
  padding: 0;
  list-style: none;
  text-align: inherit;
}
.reveal .act li { max-width: none; color: var(--ink); }
.reveal .act li + li { margin-top: 0; }
.reveal .act li::before, .reveal .act li::after { content: none; }
.reveal .act p { max-width: none; }

/* PasswordMeter — the component ships unstyled by design. */
.pwm { max-width: 30em; margin: 0 auto; }
.pwm > * + * { margin-top: var(--s2); }
.pwm-in {
  font-family: var(--mono); font-size: var(--t-lead); width: 100%;
  padding: 0.7em 0.9em; border-radius: 0.5em;
  background: var(--surface); color: var(--ink);
  border: 1px solid var(--accent-line); outline: none; box-shadow: var(--elev);
}
.pwm-in:focus { border-color: var(--accent); }
.pwm-in::placeholder { color: var(--faint); }
.pwm-meter { height: 0.5em; border-radius: 999px; background: var(--bg-2); overflow: hidden; }
.pwm-meter > i { display: block; height: 100%; width: 0; border-radius: 999px;
                 background: var(--danger); transition: width 250ms ease, background 250ms ease; }
.pwm-meter > i.hot  { background: var(--danger); }
.pwm-meter > i.warn { background: var(--caution); }
.pwm-meter > i.ok   { background: var(--safe); }
.pwm-verdict { font-family: var(--display); font-size: var(--t-h3); font-weight: 700; color: var(--faint); }
.pwm-verdict.hot  { color: var(--danger); }
.pwm-verdict.warn { color: var(--caution); }
.pwm-verdict.ok   { color: var(--safe); }
.pwm-detail { font-family: var(--mono); font-size: var(--t-small); color: var(--muted); min-height: 1.4em; }
.pwm-note { font-size: var(--t-label); color: var(--faint); letter-spacing: 0.06em; text-transform: uppercase; }

/* Title slide */
.title-slide { display: flex; flex-direction: column; align-items: center; text-align: center; }
.title-slide h1 { max-width: 14ch; }
.title-slide.is-hero h1 { font-size: 5.6em; }
.title-slide .rule { width: 2.4em; height: 3px; background: var(--accent); border-radius: 2px; }
.title-slide p { color: var(--muted); font-size: var(--t-lead); max-width: 34ch; }

/* Chapter divider — a full-bleed background numeral with the title over it. */
.reveal section.chapter {
  text-align: center;
  position: absolute !important; top: 0 !important; left: 0 !important;
  width: 100% !important; height: 100% !important; margin: 0 !important;
  display: flex !important; align-items: center; justify-content: center;
}
.reveal section.chapter .num {
  position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
  font-family: var(--display); font-weight: 700;
  font-size: 34em; line-height: 1; letter-spacing: -0.04em;
  color: var(--accent); opacity: 0.12;
  pointer-events: none; z-index: 0;
}
.reveal section.chapter .ch-title {
  position: relative; z-index: 1; display: inline-block;
  font-family: var(--display); font-weight: 700;
  font-size: clamp(2em, 7vw, 5.5em); line-height: 1.05; letter-spacing: -0.03em;
  color: var(--ink); max-width: 14ch; text-wrap: balance;
}

/* Touch: reveal's arrow controls need a real target. */
@media (pointer: coarse) {
  .reveal .controls { --controls-arrow-angle: 36deg; right: max(12px, env(safe-area-inset-right)); bottom: max(12px, env(safe-area-inset-bottom)); }
  .reveal .controls [class*="navigate-"] { width: 44px; height: 44px; }
}

@media (prefers-reduced-motion: reduce) {
  .reveal .fragment { transform: none !important; transition: opacity 120ms linear !important; }
  .reveal-viewport { transition: none; }
}

@media print {
  *, *::before, *::after { -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important; }
  .reveal .fragment { opacity: 1 !important; visibility: visible !important; }
}
`}</style>

<section data-auto-animate data-auto-animate-duration="1.1"><div className="title-slide is-hero"><h1 data-id="deck-title">Security Is Everyone’s Job</h1></div></section>

<section data-auto-animate data-auto-animate-duration="1.1"><div className="title-slide"><h1 data-id="deck-title">Security Is Everyone’s Job</h1><div className="rule"></div><p>User awareness and practices — the threats, the passwords, and the habits that actually hold</p></div></section>

<section><h2>Before we start</h2><div className="act"><Poll id="sec-warmup" question="Be honest — have you ever clicked a link you regretted?" options={["Yes, and I told someone", "Yes, and I told nobody", "No, never", "I have no idea — that is the scary part"]} /></div></section>

<section className="chapter ch-cost"><span className="num">01</span><span className="ch-title">Why this is your problem</span></section>

<section className="ch-cost"><div className="evo"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.globe }} /><div className="evo-copy"><span className="n">01</span><h2>Your attacker is not in the building</h2><p className="lead">Distance stopped being a defence.</p><ul><li className="fragment fade-up">Anyone, anywhere on the planet, can reach your machine</li><li className="fragment fade-up">They never need to see your face</li><li className="fragment fade-up">They never need to be awake — the tools run without them</li></ul></div></div></section>

<section className="ch-cost"><h2>What bad habits cost you</h2><div className="cards is-2up"><div className="card is-danger fragment fade-up"><div className="h"><span className="num">01</span>Identity theft</div><h3>Someone else becomes you</h3><p>Not for an afternoon. For years, across every institution that trusted the record.</p></div><div className="card is-danger fragment fade-up"><div className="h"><span className="num">02</span>Monetary theft</div><h3>Your money leaves</h3><p>Slowly, then all at once — and the trail ends in a jurisdiction nobody will chase.</p></div><div className="card is-danger fragment fade-up"><div className="h"><span className="num">03</span>Legal fallout</div><h3>For you and your employer</h3><p>Regulatory exposure does not stop at the person who clicked.</p></div><div className="card is-danger fragment fade-up"><div className="h"><span className="num">04</span>Termination</div><h3>Policy exists for a reason</h3><p>Ignoring it has a price, and it is usually paid by one named individual.</p></div></div></section>

<section className="ch-cost"><h2>Where they come in</h2><p className="lead">SANS ranks these as the most-attacked surfaces on a normal desk.</p><ul className="risks"><li className="fragment">Web browser</li><li className="fragment">Chat clients</li><li className="fragment">Web applications</li><li className="fragment">Excessive user rights</li></ul><p className="fine fragment">Notice what all four have in common: you use every one of them, all day, without thinking about it.</p></section>

<section className="ch-cost"><div className="evo is-flip"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.door }} /><div className="evo-copy"><span className="n">02</span><h2>Security is not safety</h2><p className="lead">Two different jobs. You only control one of them.</p><ul><li className="fragment fade-up"><strong>Security</strong> protects the machine and the data — locks, patches, firewalls</li><li className="fragment fade-up"><strong>Safety</strong> is how you behave — what you open, what you type, who you trust</li><li className="fragment fade-up">IT owns most of the first. You own all of the second.</li></ul></div></div></section>

<section className="ch-cost" data-transition="fade"><div className="pull"><blockquote>A deadbolt is <span className="hl">security</span>.<br /><br />Not opening the door is <span className="hl">safety</span>.</blockquote></div></section>

<section className="chapter ch-threat"><span className="num">02</span><span className="ch-title">Know your enemy</span></section>

<section className="ch-threat"><h2>The cast of characters</h2><div className="cards is-2up"><div className="card fragment fade-up"><div className="h"><span className="num">01</span>The cracker</div><h3>Actually good at this</h3><p>Writes the attack tools. You will never meet them, and they are not the one who hits you.</p></div><div className="card fragment fade-up"><div className="h"><span className="num">02</span>Script kiddies</div><h3>Cannot code. Can download.</h3><p>Runs someone else’s tool at you anyway. Volume, not skill.</p></div><div className="card fragment fade-up"><div className="h"><span className="num">03</span>Criminals</div><h3>It is a business</h3><p>Bots, spam, stolen cards, resale. Margins, targets, and a support desk.</p></div><div className="card fragment fade-up"><div className="h"><span className="num">04</span>The market</div><h3>Where tools get traded</h3><p>Forums and marketplaces — the reason an amateur can rent a professional attack.</p></div></div></section>

<section className="ch-threat"><h2>What you are worth, wholesale</h2><div className="stats"><div className="stat fragment fade-up"><span className="n">&#36;1–2K</span><span className="l">a ready-made malware package</span></div><div className="stat fragment fade-up"><span className="n">&#36;8</span><span className="l">to send one million emails</span></div><div className="stat fragment fade-up"><span className="n">&#36;1,000</span><span className="l">for ten thousand hijacked PCs</span></div></div><div className="pull fragment"><blockquote>You are <span className="hl">cheap</span>. That is exactly why you are worth attacking.</blockquote></div></section>

<section className="ch-threat"><div className="evo"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.virus }} /><div className="evo-copy"><span className="n">01</span><h2>Virus</h2><p className="lead">Hitches a ride.</p><ul><li className="fragment fade-up">Attaches itself to a program, file, or disk</li><li className="fragment fade-up">Runs when the host runs — then copies itself</li><li className="fragment fade-up">The payload may be a prank or a wipe. It fires eventually.</li></ul><div className="verdict is-accent fragment"><span className="dot"></span>Antivirus → system restore → reinstall. Each hurts more than the last.</div></div></div></section>

<section className="ch-threat"><div className="evo is-flip"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.worm }} /><div className="evo-copy"><span className="n">02</span><h2>Worm</h2><p className="lead">Spreads itself.</p><ul><li className="fragment fade-up">Needs no host program and no help from you</li><li className="fragment fade-up">Copies itself machine to machine across the network</li><li className="fragment fade-up">Feeds on weak passwords, open shares, unpatched systems, USB sticks</li></ul><div className="verdict is-danger fragment"><span className="dot"></span>One infected laptop on the office wifi is an office-wide problem</div></div></div></section>

<section className="ch-threat"><div className="evo"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.trojan }} /><div className="evo-copy"><span className="n">03</span><h2>Trojan &amp; logic bomb</h2><p className="lead">Lies to you.</p><ul><li className="fragment fade-up"><strong>Trojan</strong> — looks like a gift. The free game is fun, and it mails your saved passwords out the back door.</li><li className="fragment fade-up"><strong>Logic bomb</strong> — legitimate-looking code waiting for a condition. The fired admin’s “delete everything the day my account dies” script.</li></ul><div className="verdict is-danger fragment"><span className="dot"></span>Free, cracked, and “just this once” are the three most expensive words in software</div></div></div></section>

<section className="ch-threat"><div className="evo is-flip"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.social }} /><div className="evo-copy"><span className="n">04</span><h2>Social engineering</h2><p className="lead">Hacks you, not the machine.</p><ul><li className="fragment fade-up"><em>“Hi, John from IT — quick password check?”</em></li><li className="fragment fade-up"><em>“Your bank noticed a problem with your account…”</em></li><li className="fragment fade-up"><em>“So fun! What was your mother’s maiden name?”</em></li><li className="fragment fade-up"><em>“Here to repair the machine — I have got some patches.”</em></li></ul><div className="verdict is-accent fragment"><span className="dot"></span>No exploit needed. They exploit politeness.</div></div></div></section>

<section className="ch-threat"><div className="evo"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.rootkit }} /><div className="evo-copy"><span className="n">05</span><h2>Rootkit</h2><p className="lead">Hides the evidence.</p><ul><li className="fragment fade-up">A toolkit installed <em>after</em> they are already in</li><li className="fragment fade-up">Opens a permanent back door — for them and their friends</li><li className="fragment fade-up">Logs your keystrokes and modifies the OS so you cannot see it</li></ul><div className="verdict is-danger fragment"><span className="dot"></span>The scary part is not the break-in. It is that the alarm was rewired.</div></div></div></section>

<section className="ch-threat"><div className="evo is-flip"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.botnet }} /><div className="evo-copy"><span className="n">06</span><h2>Botnet</h2><p className="lead">Rents you out.</p><ul><li className="fragment fade-up">Thousands of compromised machines under one handler</li><li className="fragment fade-up">Sends spam and malware on your behalf</li><li className="fragment fade-up">Floods targets offline, and hosts illegal content from your address</li></ul><div className="verdict is-danger fragment"><span className="dot"></span>The traffic is yours. So is the liability.</div></div></div></section>

<section className="ch-threat"><div className="evo"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.mitm }} /><div className="evo-copy"><span className="n">07</span><h2>Man in the middle</h2><p className="lead">Sits between you and where you meant to go.</p><ul><li className="fragment fade-up">The attacker impersonates your destination — an access point, a server</li><li className="fragment fade-up">You connect to <em>them</em>; they pass it along and read everything in transit</li><li className="fragment fade-up"><em>“Airport_Free_WiFi” is not run by the airport</em></li></ul><div className="verdict is-accent fragment"><span className="dot"></span>Defence: https everywhere, and a VPN on networks you do not own</div></div></div></section>

<section className="ch-threat"><h2>Checkpoint</h2><div className="act"><Quiz id="sec-threats" question="Which one spreads on its own, with no help from you at all?" options={["A virus", "A worm", "A trojan horse", "A logic bomb"]} answer={1} /></div></section>

<section className="chapter ch-decep"><span className="num">03</span><span className="ch-title">The art of asking nicely</span></section>

<section className="ch-decep"><h2>Spot the phish</h2><div className="mail"><div className="hdr"><b>FROM</b> eBay Billing Department &lt;<span className="flag">billing@ebay-secure-update.com</span>&gt;<br /><b>SUBJECT</b> Credit/debit card update</div><p>Dear <span className="flag">eBay customer</span>,</p><p>During our regularly scheduled account maintenance we detected a slight error in your billing information. <span className="flag">You must verify within 24 hours or your account will be suspended.</span></p><p>Please <span className="flag">click here</span> to confirm your card number and password.</p><p>Thank you, eBay Billing</p></div><ul className="risks fragment"><li>Wrong domain</li><li>Generic greeting</li><li>Manufactured urgency</li><li>A link, not a typed address</li><li>Asks for secrets</li></ul><p className="caption-strong fragment">Five red flags. It took four seconds.</p></section>

<section className="ch-decep"><div className="evo"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.phish }} /><div className="evo-copy"><span className="n">01</span><h2>Phishing becomes pharming</h2><p className="lead">The email is only the delivery. The website is the trap.</p><ul><li className="fragment fade-up"><strong>Phishing</strong> — a “trustworthy” sender asks for your NID, card number, or login</li><li className="fragment fade-up"><strong>Pharming</strong> — the link lands on a perfect clone of the real site</li><li className="fragment fade-up">You type your credentials into their form, and it politely forwards you to the real site afterwards</li></ul><div className="verdict is-accent fragment"><span className="dot"></span>Never reach your bank through a link. Type it, or use your own bookmark.</div></div></div></section>

<section className="ch-decep"><h2>Quick call: what do you do?</h2><p className="lead">A caller says he is from the helpdesk, gets your manager’s name right, and needs your password to “finish the migration tonight.”</p><div className="act"><Quiz id="sec-helpdesk" question="He knows your manager’s name. Now what?" options={["Give it — he knew my manager’s name.", "Give a wrong password to test him.", "Hang up. Call IT back on the number you already have.", "Email it instead, so there is a record."]} answer={2} /></div><p className="fine">Knowing an internal detail is the <em>cheapest</em> thing in the world to fake.</p></section>

<section className="chapter ch-pass"><span className="num">04</span><span className="ch-title">The one control you own</span></section>

<section className="ch-pass"><h2>How fast does yours fall?</h2><table><thead><tr><th>Pattern</th><th>Possibilities</th><th>Time to guess</th></tr></thead><tbody><tr><td>Your interests, pets, relatives</td><td className="num">20</td><td className="is-danger">5 minutes, by hand</td></tr><tr><td>Just asking you nicely</td><td className="num">1</td><td className="is-danger">2 minutes</td></tr><tr><td>A dictionary word</td><td className="num">80,000</td><td className="is-danger">under a second</td></tr><tr><td>8 characters, alphanumeric</td><td className="num">2×10¹⁴</td><td className="is-danger">3.4 minutes</td></tr><tr><td>8 characters, full keyboard</td><td className="num">7×10¹⁵</td><td className="is-caution">2 hours</td></tr><tr><td>12 characters, alphanumeric</td><td className="num">3×10²¹</td><td className="is-safe">96 years</td></tr><tr><td>12 characters, full keyboard</td><td className="num">5×10²³</td><td className="is-safe">centuries</td></tr></tbody></table><p className="fine">Assumes 2.6×10¹⁸ guesses per month.</p></section>

<section className="ch-pass"><div className="evo is-flip"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.keylen }} /><div className="evo-copy"><span className="n">01</span><h2>Complexity helps. Length wins.</h2><p className="lead">Every character you add multiplies the search space. Every symbol only widens it.</p><ul><li className="fragment fade-up">Adding one character beats adding one symbol — every time</li><li className="fragment fade-up">A long passphrase is easier to remember <em>and</em> harder to crack</li><li className="fragment fade-up">Twelve is the floor, not the target</li></ul></div></div></section>

<section className="ch-pass"><h2>Try it — nothing leaves this page</h2><PasswordMeter /></section>

<section className="ch-pass"><h2>Build one you will actually remember</h2><div className="cards is-3up"><div className="card fragment fade-up"><div className="h"><span className="num">01</span>Two unrelated words</div><p>mail + phone</p><p><span className="mono">m@!lf0n3</span></p></div><div className="card fragment fade-up"><div className="h"><span className="num">02</span>Abbreviate a phrase</div><p>“My favourite colour is blue”</p><p><span className="mono">Mfcib1ue!</span></p></div><div className="card fragment fade-up"><div className="h"><span className="num">03</span>Take a song lyric</div><p>Happy birthday, dear John</p><p><span className="mono">hb2uhb2uhbdJhb2u</span></p></div></div><p className="fragment">Or take one phrase and mutate it — <span className="mono">Merry Christmas</span> becomes <span className="mono">MXemrays</span> by intertwining, <span className="mono">M5rryXm1s</span> by swapping vowels for digits, <span className="mono">mErcHr2yOu</span> by abbreviating.</p></section>

<section className="ch-pass"><h2>Rules that survive contact</h2><ol className="flow cols" style={{ "--rows": 3 }}><li className="fragment fade-up"><span className="t">Private</span><span className="d">One person knows it. One.</span></li><li className="fragment fade-up"><span className="t">Secret</span><span className="d">Not in a file, not in a script, not under the keyboard.</span></li><li className="fragment fade-up"><span className="t">Memorable</span><span className="d">So you never need to write it down.</span></li><li className="fragment fade-up"><span className="t">Long and mixed</span><span className="d">12+ characters across upper, lower, digits, punctuation.</span></li><li className="fragment fade-up"><span className="t">Unguessable</span><span className="d">Not by a person, not by a program running all week.</span></li><li className="fragment fade-up"><span className="t">Never reused</span><span className="d">One breach elsewhere should not become a breach here.</span></li></ol><ul className="risks fragment"><li>Never admin, root, or administrator as a login name</li><li>People watch you type</li><li>A password typed into the username box lands in the logs in plain text</li></ul></section>

<section className="chapter ch-owned"><span className="num">05</span><span className="ch-title">Am I already owned</span></section>

<section className="ch-owned"><h2>Signs of a break-in</h2><div className="cards is-3up is-tight"><div className="card fragment fade-up"><h3>Antivirus speaks up</h3><p>It flagged something and you clicked past it.</p></div><div className="card fragment fade-up"><h3>Pop-ups return</h3><p>Often selling you “security software”.</p></div><div className="card fragment fade-up"><h3>Disk space vanishes</h3><p>Something is storing something.</p></div><div className="card fragment fade-up"><h3>Records you did not create</h3><p>Files, emails, transactions.</p></div><div className="card fragment fade-up"><h3>Everything crawls</h3><p>Your machine is busy working for someone else.</p></div><div className="card fragment fade-up"><h3>The pointer moves</h3><p>Without you. Shut it down and call IT.</p></div><div className="card fragment fade-up"><h3>It powers itself off</h3><p>Unprompted restarts and shutdowns.</p></div><div className="card fragment fade-up"><h3>The laptop is gone</h3><p>One in ten are stolen in their lifetime.</p></div><div className="card is-accent fragment fade-up"><h3>Nothing at all</h3><p>The most common symptom by far.</p></div></div></section>

<section className="ch-owned"><h2>Spyware tells on itself</h2><ol className="flow cols" style={{ "--rows": 3 }}><li className="fragment fade-up"><span className="t">Your homepage changed</span><span className="d">And you did not change it.</span></li><li className="fragment fade-up"><span className="t">Searches land somewhere else</span><span className="d">Results you never chose.</span></li><li className="fragment fade-up"><span className="t">The firewall switched itself off</span><span className="d">Nothing legitimate does this.</span></li><li className="fragment fade-up"><span className="t">Traffic while you are idle</span><span className="d">Heavy network activity, doing nothing.</span></li><li className="fragment fade-up"><span className="t">New icons and bookmarks</span><span className="d">Programs you did not install.</span></li><li className="fragment fade-up"><span className="t">Unknown programs calling out</span><span className="d">The firewall keeps asking about them.</span></li></ol><div className="verdict is-danger fragment"><span className="dot"></span>Any one of these: stop, disconnect, call IT. Do not “see if it clears up.”</div></section>

<section className="chapter ch-defend"><span className="num">06</span><span className="ch-title">Defend yourself</span></section>

<section className="ch-defend"><h2>Stack the defences</h2><div className="act"><DragDrop id="sec-layers" prompt="Order these from the outside in — what an attacker has to beat first, and what is left last." items={["Router / hardware firewall", "OS / software firewall", "Antivirus and least privilege", "You, not clicking the thing"]} /></div></section>

<section className="ch-defend"><div className="evo"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.layers }} /><div className="evo-copy"><span className="n">01</span><h2>Defense in depth</h2><p className="lead">No single control holds. Make the attacker beat all of them.</p><ul><li className="fragment fade-up">Each ring is cheap on its own and weak on its own</li><li className="fragment fade-up">Together they turn one mistake into a survivable one</li><li className="fragment fade-up">The innermost layer is the only one that thinks</li></ul><div className="verdict is-accent fragment"><span className="dot"></span>Every ring buys time. You decide what happens with it.</div></div></div></section>

<section className="ch-defend"><h2>The unglamorous four</h2><div className="cards is-2up"><div className="card fragment fade-up"><div className="h"><span className="num">01</span>Antivirus and antispyware</div><p>Install it, then keep it <strong>updated</strong> — yesterday’s signatures catch yesterday’s malware. Free options are fine.</p></div><div className="card fragment fade-up"><div className="h"><span className="num">02</span>Firewall</div><p>A wall between your machine and the internet. It filters what comes in, and just as importantly, what goes out.</p></div><div className="card fragment fade-up"><div className="h"><span className="num">03</span>Patch everything</div><p>Turn on automatic updates. An unapplied patch is a published instruction manual for attacking you.</p></div><div className="card fragment fade-up"><div className="h"><span className="num">04</span>Do not run as admin</div><p>Your daily account should be boring and powerless. Elevate only when the task genuinely needs it.</p></div></div></section>

<section className="ch-defend"><h2>Email and downloads</h2><ol className="flow"><li className="fragment fade-up"><span className="t">Do not open attachments unless you were expecting them</span><span className="d">Expecting it <em>and</em> trusting the sender. Both, not either.</span></li><li className="fragment fade-up"><span className="t">Do not click links unless you are certain where they go</span><span className="d">Hover first and read the actual destination, not the text.</span></li><li className="fragment fade-up"><span className="t">Download software only from sources you trust</span><span className="d">The vendor’s own site, or your organisation’s software centre.</span></li></ol><div className="pull fragment"><blockquote>“But it came from my colleague” — so did the <span className="hl">worm</span> that read their address book.</blockquote></div></section>

<section className="ch-defend"><h2>Other tricks worth knowing</h2><ul><li className="fragment fade-up">Run a pop-up blocker, and assume some get through anyway</li><li className="fragment fade-up">Close a suspicious pop-up with the window control in the corner. <span className="dim">Never “Yes”, never “Accept”, never even “Cancel” — the whole box may be a picture of a box.</span></li><li className="fragment fade-up">Found a USB stick in the car park? <strong>It was left there on purpose.</strong> Hand it to IT.</li><li className="fragment fade-up">Banking and business: check for <span className="mono">https</span> and the padlock, every single time</li><li className="fragment fade-up">On a shared machine, clear temp files, cookies, history and saved passwords before you walk away</li></ul></section>

<section className="ch-defend"><div className="evo is-flip"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.backup }} /><div className="evo-copy"><span className="n">02</span><h2>Back up before you need to</h2><p className="lead">No security measure is 100%. Plan for the day one fails.</p><ul><li className="fragment fade-up">Ask what would genuinely hurt to lose — then ask about <em>that</em> backup</li><li className="fragment fade-up">Recent · off-site · documented · encrypted · automatic</li><li className="fragment fade-up">And tested, because a restore you have never run is a guess</li></ul><div className="verdict is-danger fragment"><span className="dot"></span>An untested backup is a rumour</div></div></div></section>

<section className="chapter ch-gov"><span className="num">07</span><span className="ch-title">When it goes wrong</span></section>

<section className="ch-gov"><h2>Data that carries legal weight</h2><p className="lead">Under the Cyber Security Act 2023 and Bangladesh Bank’s ICT Security Guideline, this is the data you are accountable for.</p><div className="cards is-3up is-tight"><div className="card fragment fade-up"><h3>NID number</h3><p>The single key to every other record.</p></div><div className="card fragment fade-up"><h3>Passport or driving licence</h3><p>Government-issued identity documents.</p></div><div className="card fragment fade-up"><h3>Account plus PIN</h3><p>Bank or MFS credentials together.</p></div><div className="card fragment fade-up"><h3>Biometric data</h3><p>Fingerprint and face. You cannot reissue these.</p></div><div className="card fragment fade-up"><h3>Health records</h3><p>Status, treatment, and payment history.</p></div><div className="card fragment fade-up"><h3>TIN and tax records</h3><p>Financial identity, end to end.</p></div></div><div className="verdict is-danger fragment"><span className="dot"></span>Lose these and disclosure is not optional — the regulator, every affected person, and the bill</div></section>

<section className="ch-gov"><h2>The fraud problem</h2><div className="stats"><div className="stat fragment fade-up"><span className="n">5–6%</span><span className="l">of annual revenue lost to internal fraud</span></div><div className="stat fragment fade-up"><span className="n">18mo</span><span className="l">average scheme duration before discovery</span></div><div className="stat fragment fade-up"><span className="n">&#36;159K</span><span className="l">average cost per scheme</span></div><div className="stat fragment fade-up"><span className="n">25%</span><span className="l">of cases exceed one million dollars</span></div></div><p className="fragment">Smaller companies lose <strong>more per incident</strong>, not less.</p><p className="fine">Figures from US-based research: <em>Essentials of Corporate Fraud</em>, T. L. Coenen, 2008, John Wiley &amp; Sons.</p></section>

<section className="ch-gov"><h2>How fraud actually gets found</h2><div className="lanes"><div className="lane is-top fragment fade-up"><span>Tip</span><span className="track"><i style={{ width: "100%" }}></i></span><span className="v">34%</span></div><div className="lane fragment fade-up"><span>By accident</span><span className="track"><i style={{ width: "74%" }}></i></span><span className="v">25%</span></div><div className="lane fragment fade-up"><span>Internal audit</span><span className="track"><i style={{ width: "59%" }}></i></span><span className="v">20%</span></div><div className="lane fragment fade-up"><span>Internal controls</span><span className="track"><i style={{ width: "56%" }}></i></span><span className="v">19%</span></div><div className="lane fragment fade-up"><span>External audit</span><span className="track"><i style={{ width: "35%" }}></i></span><span className="v">12%</span></div><div className="lane fragment fade-up"><span>Police</span><span className="track"><i style={{ width: "12%" }}></i></span><span className="v">4%</span></div></div><p className="fine fragment">Tips come from coworkers 64% · anonymous 18% · customers 11% · vendors 7%</p></section>

<section className="ch-gov"><div className="evo"><div className="evo-art" dangerouslySetInnerHTML={{ __html: SVGS.speak }} /><div className="evo-copy"><span className="n">01</span><h2>The best detection system is a person</h2><p className="lead">More fraud is caught by someone saying something than by every audit and control combined.</p><ul><li className="fragment fade-up">You do not need proof — you need to raise it</li><li className="fragment fade-up">Two thirds of tips come from a coworker, not an outsider</li></ul><div className="verdict is-accent fragment"><span className="dot"></span>Suspect fraud? Contact <span className="mono">[ your reporting contact here ]</span></div></div></div></section>

<section><h2>If you remember six things</h2><ol className="flow cols" style={{ "--rows": 3 }}><li className="fragment fade-up"><span className="t">Long passwords</span><span className="d">Never reused, never shared.</span></li><li className="fragment fade-up"><span className="t">Patch and update</span><span className="d">Automatically, so you never have to decide.</span></li><li className="fragment fade-up"><span className="t">Do not click — type</span><span className="d">Reach important sites through your own bookmark.</span></li><li className="fragment fade-up"><span className="t">Nobody legitimate asks for your password</span><span className="d">Not IT, not your bank, not your manager.</span></li><li className="fragment fade-up"><span className="t">Back it up, then test the restore</span><span className="d">Untested is untrue.</span></li><li className="fragment fade-up"><span className="t">See something odd? Say it today</span><span className="d">The tip is the control that works.</span></li></ol></section>

<section><h2>One thing, this week</h2><div className="act"><Poll id="sec-commit" question="What are you actually going to change before Friday?" options={["Replace a password I reuse everywhere", "Turn on automatic updates", "Set up a backup — and test it", "Stop opening links in email"]} /></div></section>

<section><div className="title-slide"><h1>Security is everyone’s responsibility</h1><div className="rule"></div><p>Yours included. Especially yours.</p><p className="fine">Most of this is NIST best practice. It applies at work and at home — your employer’s policies exist for the same reason.</p></div></section>
